---
phase: 05-cloudinary-storage-build-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - index.html
  - package.json
autonomous: false
requirements:
  - BUILD-03
  - BUILD-04

must_haves:
  truths:
    - "Gallery renders identically with Cloudinary URLs -- no visual changes to public site"
    - "BTS and social wall images load correctly after gallery image cleanup"
    - "Public site JS bundle remains under 80KB gzip"
    - "Gallery component code is unchanged"
  artifacts:
    - path: "index.html"
      provides: "Updated BTS and social wall image paths"
    - path: "src/components/gallery.js"
      provides: "Unchanged gallery component"
  key_links:
    - from: "src/components/gallery.js"
      to: "src/data/gallery-images.js"
      via: "import { galleryImages, categories }"
      pattern: "import.*galleryImages.*gallery-images"
    - from: "index.html"
      to: "Cloudinary CDN"
      via: "img src attributes"
      pattern: "res\\.cloudinary\\.com"
---

<objective>
Update BTS and social wall image paths so they survive the gallery folder cleanup, verify the public gallery renders identically with zero code changes, and confirm the JS bundle stays under 80KB gzip.

Purpose: Complete the migration by ensuring nothing breaks when old gallery images are removed from git, and verify all success criteria.
Output: Working site with all images served correctly, bundle size verified.
</objective>

<execution_context>
@/Users/davidbradley/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidbradley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cloudinary-storage-build-pipeline/05-CONTEXT.md
@.planning/phases/05-cloudinary-storage-build-pipeline/05-RESEARCH.md
@.planning/phases/05-cloudinary-storage-build-pipeline/05-01-SUMMARY.md
@.planning/phases/05-cloudinary-storage-build-pipeline/05-02-SUMMARY.md

<interfaces>
<!-- BTS and Social Wall image references in index.html that will break when public/images/gallery/ is removed -->

From index.html (13 hardcoded img tags referencing /images/gallery/):

BTS section (4 images):
- /images/gallery/track-rally-spec-03.webp
- /images/gallery/jdm-drift-machine-03.webp
- /images/gallery/supercar-carbon-fiber-05.webp
- /images/gallery/euro-classic-roadster-05.webp

Social wall section (9 images):
- /images/gallery/supercar-exotic-red-01.webp
- /images/gallery/jdm-street-racer-01.webp
- /images/gallery/euro-sports-coupe-01.webp
- /images/gallery/american-muscle-pony-car-01.webp
- /images/gallery/track-circuit-racer-01.webp
- /images/gallery/jdm-tuner-coupe-02.webp
- /images/gallery/supercar-hypercar-02.webp
- /images/gallery/euro-hot-hatch-04.webp
- /images/gallery/track-time-attack-02.webp

From src/components/gallery.js (must NOT change):
```javascript
import { galleryImages, categories } from '../data/gallery-images.js';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update BTS and social wall image paths to Cloudinary CDN URLs</name>
  <files>index.html, package.json</files>
  <read_first>
    - index.html
    - package.json
    - .planning/phases/05-cloudinary-storage-build-pipeline/05-01-SUMMARY.md
  </read_first>
  <action>
Update all 13 hardcoded `<img>` src attributes in index.html that reference `/images/gallery/*.webp` to use Cloudinary CDN URLs instead. This is necessary because `public/images/gallery/` will be removed from git after migration.

The Cloudinary URL pattern is:
```
https://res.cloudinary.com/{CLOUD_NAME}/image/upload/f_auto,q_auto,w_800,c_limit/{public_id}
```

Use `w_800,c_limit` for BTS and social images (they display smaller than gallery images, so 800px width is sufficient).

Get the CLOUD_NAME from the 05-01-SUMMARY.md (the user provided it during the checkpoint).

**BTS section** (4 images) -- update src attributes:
- `track-rally-spec-03.webp` -> `https://res.cloudinary.com/{CLOUD}/image/upload/f_auto,q_auto,w_800,c_limit/gallery/track/track-rally-spec-03`
- `jdm-drift-machine-03.webp` -> `https://res.cloudinary.com/{CLOUD}/image/upload/f_auto,q_auto,w_800,c_limit/gallery/jdm/jdm-drift-machine-03`
- `supercar-carbon-fiber-05.webp` -> `https://res.cloudinary.com/{CLOUD}/image/upload/f_auto,q_auto,w_800,c_limit/gallery/supercar/supercar-carbon-fiber-05`
- `euro-classic-roadster-05.webp` -> `https://res.cloudinary.com/{CLOUD}/image/upload/f_auto,q_auto,w_800,c_limit/gallery/euro/euro-classic-roadster-05`

**Social wall section** (9 images) -- update src attributes:
- `supercar-exotic-red-01.webp` -> `.../gallery/supercar/supercar-exotic-red-01`
- `jdm-street-racer-01.webp` -> `.../gallery/jdm/jdm-street-racer-01`
- `euro-sports-coupe-01.webp` -> `.../gallery/euro/euro-sports-coupe-01`
- `american-muscle-pony-car-01.webp` -> `.../gallery/american-muscle/american-muscle-pony-car-01`
- `track-circuit-racer-01.webp` -> `.../gallery/track/track-circuit-racer-01`
- `jdm-tuner-coupe-02.webp` -> `.../gallery/jdm/jdm-tuner-coupe-02`
- `supercar-hypercar-02.webp` -> `.../gallery/supercar/supercar-hypercar-02`
- `euro-hot-hatch-04.webp` -> `.../gallery/euro/euro-hot-hatch-04`
- `track-time-attack-02.webp` -> `.../gallery/track/track-time-attack-02`

Keep the `loading="lazy"` attribute and existing alt text on all images. Do NOT change the hero image (`/images/hero-placeholder.webp`) -- it stays local for LCP performance.

After updating, remove the `public/images/gallery/` directory from the working tree. Run `git rm -r public/images/gallery/` if the files are tracked, or simply delete the directory if untracked.

Also remove the `sharp` devDependency from package.json since it is no longer needed (the Sharp-based pipeline is replaced by Cloudinary). Run `npm uninstall sharp`. Keep `process-images.js` in the repo for now as reference (can be cleaned up later).
  </action>
  <verify>
    <automated>! grep -q '"/images/gallery/' index.html && grep -q 'res.cloudinary.com' index.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - No `<img>` tags in index.html reference `/images/gallery/` paths
    - All 13 BTS and social wall images use `res.cloudinary.com` CDN URLs
    - Hero image at `/images/hero-placeholder.webp` is unchanged (still local)
    - `public/images/gallery/` directory is removed
    - `sharp` is no longer in devDependencies
  </acceptance_criteria>
  <done>All 13 BTS/social image paths updated to Cloudinary CDN URLs, gallery folder removed from git, sharp dependency removed</done>
</task>

<task type="auto">
  <name>Task 2: Verify bundle size and gallery component unchanged</name>
  <files>none</files>
  <read_first>
    - src/components/gallery.js
    - package.json
  </read_first>
  <action>
1. **Verify gallery.js is unchanged:** Confirm `src/components/gallery.js` has NOT been modified during this phase. It should still import from `'../data/gallery-images.js'` and use `galleryImages` and `categories` exactly as before. Run `git diff src/components/gallery.js` -- output should be empty.

2. **Run full build:**
```bash
npm run build
```
This runs prebuild (generates gallery data from Cloudinary) then Vite build.

3. **Check JS bundle size:**
```bash
ls -la dist/assets/*.js
```
Total gzipped JS must be under 80KB. To check gzip size:
```bash
gzip -c dist/assets/*.js | wc -c
```
The result divided by 1024 should be under 80.

4. **Verify no Cloudinary SDK in bundle:** The cloudinary package should NOT appear in the built output:
```bash
grep -r "cloudinary" dist/ || echo "No cloudinary references in bundle - GOOD"
```

5. **Verify generated gallery-images.js has 29 entries:**
```bash
grep -c "isPlaceholder: false" src/data/gallery-images.js
```
Should output `29`.

6. **Log all verification results** to console for the summary.
  </action>
  <verify>
    <automated>npm run build && gzip -c dist/assets/*.js | wc -c | awk '{if ($1/1024 < 80) print "BUNDLE OK: " $1/1024 "KB gzip"; else print "BUNDLE TOO LARGE: " $1/1024 "KB gzip"; exit ($1/1024 >= 80)}'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff src/components/gallery.js` shows no changes (gallery component unchanged)
    - `npm run build` succeeds without errors
    - Gzipped JS bundle is under 80KB (81920 bytes)
    - No `cloudinary` string appears in `dist/` output
    - Generated gallery-images.js has exactly 29 entries
  </acceptance_criteria>
  <done>Gallery component unchanged, full build succeeds, JS bundle under 80KB gzip, no Cloudinary SDK in public bundle</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of live gallery</name>
  <files>none</files>
  <action>
This is a human-verify checkpoint. Claude starts the dev server and the user visually confirms the gallery, BTS, and social sections render correctly with Cloudinary images.
  </action>
  <what-built>Complete Cloudinary migration -- gallery images now load from CDN, BTS and social wall images updated, build pipeline generates data automatically.</what-built>
  <how-to-verify>
1. Run `npm run dev` to start the local dev server
2. Open the site in your browser
3. Verify the following:
   - Gallery section: All images load (no broken images), category filters work, lightbox opens
   - BTS section: All 4 behind-the-scenes images load correctly
   - Social wall: All 9 social grid images load correctly
   - Hero image: Still loads fast (it stayed local)
   - Overall: Site looks and works identically to before the migration
4. Check browser DevTools Network tab: gallery images should load from `res.cloudinary.com` domain
  </how-to-verify>
  <verify>
    <automated>curl -sI "https://res.cloudinary.com" | head -1 | grep -q "HTTP" && echo "PASS: Cloudinary CDN reachable"</automated>
  </verify>
  <done>User confirms gallery, BTS, and social sections render correctly with all images loading from Cloudinary CDN</done>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- All image references in index.html point to Cloudinary CDN (no local gallery paths)
- Hero image stays local at /images/hero-placeholder.webp
- Gallery component (gallery.js) has zero code changes
- npm run build succeeds end-to-end
- JS bundle under 80KB gzip
- No Cloudinary SDK code in public bundle
- Visual verification confirms gallery renders identically
</verification>

<success_criteria>
The public gallery loads all images from Cloudinary CDN with automatic format optimization. The build pipeline generates gallery data without manual steps. The gallery component required zero code changes. The JS bundle stays under 80KB gzip. BTS and social wall images load correctly from Cloudinary.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cloudinary-storage-build-pipeline/05-03-SUMMARY.md`
</output>
