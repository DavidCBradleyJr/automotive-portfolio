---
phase: 11-foundation-pipeline-validation
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified: []
autonomous: false
requirements: [V4-FOUND-01, V4-FOUND-02, V4-FOUND-03]

user_setup:
  - service: netlify
    why: "New staging site for Next.js v4.0 deployment"
    env_vars:
      - name: NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME
        source: "Same value as CLOUDINARY_CLOUD_NAME from existing site"
      - name: CLOUDINARY_CLOUD_NAME
        source: "Netlify dashboard -> Site settings -> Environment variables (copy from existing site)"
      - name: CLOUDINARY_API_KEY
        source: "Netlify dashboard -> Site settings -> Environment variables (copy from existing site)"
      - name: CLOUDINARY_API_SECRET
        source: "Netlify dashboard -> Site settings -> Environment variables (copy from existing site)"
    dashboard_config:
      - task: "Set base directory to 'next/' in site settings if deploying from monorepo"
        location: "Netlify Dashboard -> Site settings -> Build & deploy -> Build settings -> Base directory"

must_haves:
  truths:
    - "Visiting the deployed Netlify URL shows the dark-themed skeleton page"
    - "No flash of white on initial page load"
    - "Test image renders via CldImage with visible blur-up placeholder"
    - "Direct URL access to the deployed site returns 200 (not 404/500)"
    - "Deployed bundle is under 200KB gzipped"
  artifacts: []
  key_links:
    - from: "Netlify build"
      to: "next/scripts/build-gallery-data.mjs"
      via: "npm run prebuild (automatic)"
      pattern: "prebuild"
    - from: "Netlify env vars"
      to: "CldImage rendering"
      via: "NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME"
      pattern: "CLOUDINARY"
---

<objective>
Deploy the Next.js skeleton to a new Netlify staging site and verify all four phase success criteria on the live deployed URL.

Purpose: Prove the entire deployment pipeline works end-to-end: build, deploy, routing, dark theme, and Cloudinary rendering on a real Netlify URL (per D-05 -- separate staging site).
Output: A live deployed URL confirming V4-FOUND-01, V4-FOUND-02, and V4-FOUND-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundation-pipeline-validation/11-CONTEXT.md
@.planning/phases/11-foundation-pipeline-validation/11-RESEARCH.md
@.planning/phases/11-foundation-pipeline-validation/11-01-SUMMARY.md
@.planning/phases/11-foundation-pipeline-validation/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Netlify staging site and deploy</name>
  <files></files>
  <read_first>
    - /Users/davidbradley/Projects/automotive-portfolio/next/netlify.toml (deployment config)
    - /Users/davidbradley/Projects/automotive-portfolio/next/package.json (build scripts)
    - /Users/davidbradley/Projects/automotive-portfolio/.planning/phases/11-foundation-pipeline-validation/11-RESEARCH.md (Netlify deployment details and pitfalls)
  </read_first>
  <action>
1. Check if Netlify CLI is available: `which netlify || npx netlify --version`
   - If available: use CLI to create site and deploy
   - If not available: install globally or use npx

2. Create a new Netlify site for the v4 staging deployment (per D-05):
   `cd next && npx netlify sites:create --name david-bradley-v4-staging`
   (The exact name may need adjustment if taken -- any unique name works)

3. Link the `next/` directory to the new Netlify site:
   `cd next && npx netlify link`

4. Set environment variables on the Netlify site. These MUST be set before the first deploy:
   - `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME` -- same value as `CLOUDINARY_CLOUD_NAME`
   - `CLOUDINARY_CLOUD_NAME` -- for prebuild script
   - `CLOUDINARY_API_KEY` -- for prebuild script
   - `CLOUDINARY_API_SECRET` -- for prebuild script

   Use CLI: `npx netlify env:set NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME <value>` etc.
   Or instruct user to set in Netlify dashboard if CLI auth is not available.

5. Deploy using CLI: `cd next && npx netlify deploy --build --prod`
   This runs the build on Netlify's infrastructure and deploys.

   Alternative if CLI deploy fails: push to a git branch and connect the Netlify site to the repo with base directory set to `next/`.

6. Record the deployed URL for the checkpoint task.

Note: If the executor hits an authentication wall with the Netlify CLI, this becomes a checkpoint:human-action for the user to authenticate or deploy manually. The executor should attempt CLI first.
  </action>
  <verify>
    <automated>cd /Users/davidbradley/Projects/automotive-portfolio/next && npx netlify status 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - A new Netlify site exists (separate from the existing Vite site per D-05)
    - The site has all 4 Cloudinary env vars set
    - The site has been deployed successfully (deploy URL accessible)
    - `curl -s -o /dev/null -w "%{http_code}" <DEPLOYED_URL>` returns 200
  </acceptance_criteria>
  <done>Next.js skeleton deployed to a new Netlify staging site with Cloudinary env vars configured. Deploy URL is accessible and returns 200.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all phase success criteria on deployed site</name>
  <files></files>
  <action>
Human verifies the deployed Netlify staging URL against all Phase 11 success criteria. No code changes -- this is a visual and functional verification checkpoint.
  </action>
  <read_first>
    - /Users/davidbradley/Projects/automotive-portfolio/.planning/phases/11-foundation-pipeline-validation/11-CONTEXT.md (phase success criteria)
  </read_first>
  <what-built>
    Complete Phase 11 foundation: Next.js 15 skeleton with dark theme, Cloudinary CldImage rendering with LQIP blur-up, deployed to Netlify staging site.
  </what-built>
  <how-to-verify>
    Visit the deployed Netlify staging URL and verify ALL of the following:

    1. **Dark theme, no FOUC (V4-FOUND-01):** Page loads with dark background (#0e0e12) immediately. There is NO flash of white or light background on initial load. Reload the page several times to confirm.

    2. **CldImage with blur placeholder (V4-FOUND-02):** A test image from Cloudinary appears on the page. On slow connection (DevTools -> Network -> Slow 3G), you should see a blurry placeholder that transitions to the full sharp image.

    3. **Direct URL access (V4-FOUND-01):** Paste the URL directly into a new browser tab (not via navigation). The page should load correctly -- no 404, no 500, no blank page.

    4. **Typography:** "David Bradley" heading appears in Orbitron font (geometric, futuristic). "Automotive Photography" appears in Space Grotesk font.

    5. **Bundle size (V4-FOUND-03):** Open DevTools -> Network tab -> reload. Check that total JS transferred is under 200KB (look at the JS files transferred, not total page size).
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: human verification of deployed site"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms dark background loads with no FOUC
    - User confirms CldImage renders with blur-up placeholder
    - User confirms direct URL access returns the page (no 404/500)
    - User confirms Orbitron and Space Grotesk fonts are visible
    - User confirms JS bundle is under 200KB in DevTools Network tab
  </acceptance_criteria>
  <done>User has approved all 5 verification checks on the deployed Netlify staging URL.</done>
  <resume-signal>Type "approved" if all 5 checks pass, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
All verification happens in the checkpoint task. The deployed site must pass:
- Dark background with no FOUC
- CldImage rendering with blur placeholder
- Direct URL access returns 200
- Correct fonts (Orbitron + Space Grotesk)
- JS bundle under 200KB
</verification>

<success_criteria>
- Deployed Netlify staging URL is accessible
- All four ROADMAP success criteria for Phase 11 are verified on the live site:
  1. Dark background (#0e0e12) with no flash of white
  2. CldImage renders with LQIP blur-up placeholder
  3. Bundle under 200KB gzipped
  4. Direct URL access works (no 404/500)
</success_criteria>

<output>
After completion, create `.planning/phases/11-foundation-pipeline-validation/11-03-SUMMARY.md`
</output>
