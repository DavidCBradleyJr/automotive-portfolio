---
phase: 08-blog-data-pipeline-post-pages
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/blog/blog-post.css
  - src/blog/blog-post.js
autonomous: false
requirements:
  - BLOG-05
  - BLOG-07

must_haves:
  truths:
    - "Blog post page uses the dark theme (#0F0F0F background, purple accents, Orbitron headings, Space Grotesk body)"
    - "Blog post has a full-bleed cinematic cover image header with title overlay"
    - "Post body content is constrained to ~700px reading width, centered"
    - "Inline photos in the post body are clickable and open in PhotoSwipe lightbox"
    - "Nav hamburger menu works on blog pages (open/close/escape)"
    - "Post page is responsive and readable from 320px to 1440px+"
  artifacts:
    - path: "src/blog/blog-post.css"
      provides: "Blog post page styles -- hero, content column, typography, images, responsive"
      min_lines: 80
    - path: "src/blog/blog-post.js"
      provides: "Blog post JS entry -- imports CSS, initializes nav hamburger + PhotoSwipe lightbox"
      min_lines: 20
  key_links:
    - from: "src/blog/blog-post.js"
      to: "photoswipe"
      via: "PhotoSwipeLightbox import"
      pattern: "import PhotoSwipeLightbox"
    - from: "src/blog/blog-post.js"
      to: "src/blog/blog-post.css"
      via: "CSS import"
      pattern: "import.*blog-post\\.css"
    - from: "src/blog/blog-post.js"
      to: "src/tokens.css"
      via: "CSS import for design tokens"
      pattern: "import.*tokens\\.css"
    - from: "blog/*/index.html"
      to: "src/blog/blog-post.js"
      via: "script module tag"
      pattern: "src=\"/src/blog/blog-post\\.js\""
---

<objective>
Create the blog post page styles and JavaScript -- making the generated HTML look and feel like it belongs on the site with dark theme, typography, responsive layout, and PhotoSwipe lightbox for inline images.

Purpose: The raw HTML from Plan 01 needs CSS and JS to be a polished, responsive blog reading experience matching the site's cinematic dark aesthetic.
Output: blog-post.css with full post styling, blog-post.js with nav and lightbox initialization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-blog-data-pipeline-post-pages/08-CONTEXT.md
@.planning/phases/08-blog-data-pipeline-post-pages/08-RESEARCH.md
@.planning/phases/08-blog-data-pipeline-post-pages/08-01-SUMMARY.md

@src/tokens.css (design tokens to use)
@src/style.css (existing section styles, button styles for reference)
@src/components/nav.css (nav styles -- imported by blog-post.js)
@src/components/nav.js (nav init function -- reference for blog nav setup)
@src/components/footer.css (footer styles)

<interfaces>
<!-- From Plan 01 output: the generated HTML structure that CSS/JS must target -->

Blog post HTML structure (generated by scripts/build-blog.js):
```html
<article class="blog-post">
  <div class="blog-post__hero" style="background-image: url('...')">
    <div class="blog-post__hero-overlay">
      <h1 class="blog-post__title">Post Title</h1>
      <time class="blog-post__date">March 15, 2026</time>
      <div class="blog-post__tags">
        <span class="blog-post__tag">JDM</span>
        <span class="blog-post__tag">Events</span>
      </div>
    </div>
  </div>
  <div class="blog-post__content">
    <p>Rendered markdown text...</p>
    <a href="cloudinary-url" data-pswp-width="1600" data-pswp-height="1067"
       data-caption="Alt text" class="blog-post__image-link">
      <img src="cloudinary-url" alt="Alt text" loading="lazy" class="blog-post__image" />
    </a>
    <p>More text...</p>
  </div>
</article>
```

From src/tokens.css -- key tokens to use:
```css
--color-bg: #0F0F0F;
--color-text: #F5F5F5;
--color-accent: (purple accent);
--font-display: 'Orbitron', sans-serif;
--font-body: 'Space Grotesk', sans-serif;
```

From src/components/nav.js -- exported function:
```javascript
export function initNav() { /* full scroll-spy + hamburger */ }
```
Note: initNav() depends on `.hero` section for frosted glass transition and `section[id]` for scroll-spy. Blog pages don't have these -- need simplified nav init (just hamburger toggle).

From existing PhotoSwipe usage (src/components/gallery.js pattern):
```javascript
import PhotoSwipeLightbox from 'photoswipe/lightbox';
import 'photoswipe/style.css';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create blog post CSS and JS entry point</name>
  <read_first>
    - src/tokens.css (all design tokens -- colors, fonts, spacing values)
    - src/style.css (section padding patterns, button styles, heading styles)
    - src/components/nav.css (nav styles that blog page inherits)
    - src/components/nav.js (hamburger logic to adapt for blog -- no scroll-spy needed)
    - src/components/footer.css (footer styles)
    - blog/cars-and-coffee-march-2026/index.html (generated HTML to style -- verify class names)
  </read_first>
  <files>
    src/blog/blog-post.css,
    src/blog/blog-post.js
  </files>
  <action>
    1. Create `src/blog/blog-post.css` with BEM naming following project conventions:

       **Hero section:**
       - `.blog-post__hero`: full-viewport height (100vh or 70vh), background-size: cover, background-position: center, position: relative
       - `.blog-post__hero-overlay`: absolute positioned overlay with dark gradient from bottom (rgba(15,15,15,0.3) to rgba(15,15,15,0.9)), flex column, justify-content: flex-end, padding: var(--space-2xl) at bottom
       - `.blog-post__title`: font-family: var(--font-display) (Orbitron), font-weight: 700, font-size clamp(2rem, 5vw, 3.5rem), color: var(--color-text), text-shadow for readability over cover image
       - `.blog-post__date`: font-family: var(--font-body), color with slight opacity (rgba(245,245,245,0.7)), font-size: 0.9rem, margin-top: var(--space-sm), text-transform: uppercase, letter-spacing: 0.1em
       - `.blog-post__tags`: display: flex, gap: var(--space-xs), flex-wrap: wrap, margin-top: var(--space-sm)
       - `.blog-post__tag`: background: rgba(var(--color-accent-rgb, 139, 92, 246), 0.2), color: var(--color-accent), padding: 0.25em 0.75em, border-radius: 2px, font-size: 0.8rem, font-family: var(--font-body), text-transform: uppercase, letter-spacing: 0.05em

       **Content area:**
       - `.blog-post__content`: max-width: 700px (per user decision -- inline with text column ~700px), margin: 0 auto, padding: var(--space-2xl) var(--space-md), color: var(--color-text), font-family: var(--font-body), font-size: 1.125rem, line-height: 1.8
       - `.blog-post__content p`: margin-bottom: 1.5em
       - `.blog-post__content h2`: font-family: var(--font-display), font-size: 1.75rem, margin: 2em 0 0.75em, color: var(--color-text)
       - `.blog-post__content h3`: font-family: var(--font-display), font-size: 1.35rem, margin: 1.75em 0 0.5em
       - `.blog-post__content blockquote`: border-left: 3px solid var(--color-accent), padding-left: var(--space-md), margin: 1.5em 0, font-style: italic, opacity: 0.85
       - `.blog-post__content a` (text links, NOT .blog-post__image-link): color: var(--color-accent), text-decoration: underline, text-underline-offset: 3px
       - `.blog-post__content strong`: font-weight: 600
       - `.blog-post__content ul, .blog-post__content ol`: margin-bottom: 1.5em, padding-left: 1.5em
       - `.blog-post__content li`: margin-bottom: 0.5em

       **Inline images:**
       - `.blog-post__image-link`: display: block, margin: 2em -2rem (slightly wider than text column on desktop, for visual punch), cursor: zoom-in. On mobile (< 640px), margin: 2em -1rem
       - `.blog-post__image`: width: 100%, height: auto, border-radius: 4px, display: block
       - `.blog-post__image-link:hover .blog-post__image`: opacity: 0.9, transition: opacity 0.2s ease

       **Responsive:**
       - Mobile (max-width: 640px): hero height 50vh, title font-size smaller, content padding reduced, image margin reduced
       - Tablet (641-1024px): hero 60vh
       - Desktop (1025px+): hero 70vh, content with comfortable side padding

       **Prefers-reduced-motion:** Remove hover transitions

       **Ensure dark theme:** body background #0F0F0F, color #F5F5F5 (may already come from tokens.css import, but add as fallback on `.blog-post` or body in this stylesheet)

    2. Create `src/blog/blog-post.js`:
       - Import CSS in this order:
         ```javascript
         import '../tokens.css';
         import '../style.css';
         import '../components/nav.css';
         import '../components/footer.css';
         import './blog-post.css';
         import 'photoswipe/style.css';
         ```
       - Import `PhotoSwipeLightbox` from `'photoswipe/lightbox'`
       - Create `initBlogNav()` function:
         - Simplified version of nav.js -- only hamburger toggle + overlay open/close + escape key + focus trapping
         - NO scroll-spy (blog pages don't have sections to spy on)
         - NO frosted glass hero observer (blog pages have their own hero)
         - Add `nav--scrolled` class immediately (blog pages should always show frosted glass nav since there's no transparent hero state)
         - Query `.nav__hamburger`, `.nav__overlay`, `.nav__overlay-link`, `.nav__overlay-cta`
         - Implement open/close with `no-scroll` body class, `nav__hamburger--open`, `nav__overlay--open` classes
         - Close on overlay link click, CTA click, Escape key
         - Focus trapping within overlay (same as nav.js)
       - Create `initBlogLightbox()` function:
         - Initialize `PhotoSwipeLightbox` with:
           - `gallery: '.blog-post__content'`
           - `children: 'a.blog-post__image-link'`
           - `pswpModule: () => import('photoswipe')`
           - `bgOpacity: 0.95`
           - `loop: false` (blog images are sequential, not a gallery)
         - Call `lightbox.init()`
       - Call both functions at module level:
         ```javascript
         initBlogNav();
         initBlogLightbox();
         ```
  </action>
  <verify>
    <automated>cd /Users/davidbradley/Projects/automotive-portfolio && node scripts/build-blog.js && npm run build 2>&1 | tail -5 && test -f dist/blog/cars-and-coffee-march-2026/index.html && grep -q "blog-post" dist/blog/cars-and-coffee-march-2026/index.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `src/blog/blog-post.css` exists with styles for `.blog-post__hero`, `.blog-post__hero-overlay`, `.blog-post__title`, `.blog-post__date`, `.blog-post__tags`, `.blog-post__content`, `.blog-post__image-link`, `.blog-post__image`
    - `.blog-post__content` has `max-width: 700px` (user decision: ~700px reading width)
    - `.blog-post__hero-overlay` has dark gradient overlay
    - `.blog-post__image-link` has `cursor: zoom-in`
    - Responsive breakpoints at 640px and 1024px
    - `prefers-reduced-motion` media query present
    - `src/blog/blog-post.js` imports tokens.css, style.css, nav.css, footer.css, blog-post.css, photoswipe/style.css
    - `src/blog/blog-post.js` imports PhotoSwipeLightbox from 'photoswipe/lightbox'
    - `initBlogNav()` adds `nav--scrolled` class and handles hamburger toggle
    - `initBlogLightbox()` targets `.blog-post__content` gallery with `a.blog-post__image-link` children
    - `npm run build` succeeds with blog assets included in dist/
  </acceptance_criteria>
  <done>Blog post pages have full dark-theme styling, responsive layout with ~700px content width, clickable images open PhotoSwipe lightbox, nav hamburger works</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of blog post page</name>
  <files>none (verification only)</files>
  <action>
    Build the site and launch preview server for visual inspection:
    `cd /Users/davidbradley/Projects/automotive-portfolio && node scripts/build-blog.js && npx vite build && npx vite preview`
    Then guide user through verification steps.
  </action>
  <what-built>
    Complete blog post page with:
    - Full-bleed cinematic cover image with title overlay
    - Dark theme matching the main site (Orbitron headings, Space Grotesk body)
    - ~700px reading column with markdown-rendered content
    - Inline photos that open in PhotoSwipe lightbox on click
    - Responsive layout from mobile to desktop
    - Working nav with hamburger menu and Blog link
    - Footer with absolute links back to main site sections
  </what-built>
  <how-to-verify>
    1. Run: `cd /Users/davidbradley/Projects/automotive-portfolio && node scripts/build-blog.js && npx vite build && npx vite preview`
    2. Open browser to http://localhost:4173/blog/cars-and-coffee-march-2026
    3. Verify:
       - Cover image fills hero area with dark gradient overlay
       - Title "Cars & Coffee March 2026" in Orbitron font overlaid on cover
       - Date and tags visible below title
       - Scroll down: body text in Space Grotesk, ~700px wide, centered
       - Inline photos visible, click one -- PhotoSwipe lightbox opens
       - Nav bar at top with "Blog" link, hamburger works on mobile (resize to 375px)
       - Footer at bottom with links to /#gallery, /#about, etc.
       - Dark theme throughout (#0F0F0F background, light text)
    4. Check http://localhost:4173/blog/first-track-day -- should 404 (draft)
    5. Check http://localhost:4173/ -- main site still works normally
  </how-to-verify>
  <verify>
    <automated>cd /Users/davidbradley/Projects/automotive-portfolio && npm run build 2>&1 | tail -3 && echo "Build succeeded -- ready for visual check"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms blog post page looks correct with dark theme, cover hero, readable content
    - User confirms PhotoSwipe lightbox works on inline images
    - User confirms nav and footer are functional
    - User confirms main site still works at /
  </acceptance_criteria>
  <done>User has approved the visual design of the blog post page</done>
  <resume-signal>Type "approved" or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with blog CSS/JS bundled
2. Blog post page matches dark theme with Orbitron headings and Space Grotesk body
3. Cover image hero is full-bleed with gradient overlay and title
4. Content column is ~700px max-width
5. Clicking inline photos opens PhotoSwipe lightbox
6. Nav hamburger works on mobile
7. Responsive from 320px to 1440px+
8. Main site unaffected
</verification>

<success_criteria>
- Blog post at /blog/cars-and-coffee-march-2026 renders with cinematic dark theme
- PhotoSwipe lightbox works on inline images
- Responsive from mobile to desktop
- Nav and footer functional with absolute links
- Main site still works
- User visually approves the design
</success_criteria>

<output>
After completion, create `.planning/phases/08-blog-data-pipeline-post-pages/08-02-SUMMARY.md`
</output>
