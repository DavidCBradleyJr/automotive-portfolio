---
phase: 08-blog-data-pipeline-post-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - content/blog/cars-and-coffee-march-2026.md
  - content/blog/first-track-day.md
  - scripts/build-blog.js
  - vite.config.js
  - .gitignore
autonomous: true
requirements:
  - BDATA-01
  - BDATA-02
  - BDATA-03
  - BDATA-04
  - BLOG-02
  - BLOG-03

must_haves:
  truths:
    - "Running `node scripts/build-blog.js` generates blog/cars-and-coffee-march-2026/index.html and blog/first-track-day/index.html"
    - "Generated HTML contains the post title, formatted date, cover image, tags, and rendered markdown body"
    - "Draft posts are skipped -- only status: published posts produce HTML files"
    - "Images in markdown are wrapped in PhotoSwipe-compatible <a> tags with data-pswp attributes"
    - "Running `npm run build` succeeds and produces blog pages in dist/blog/"
  artifacts:
    - path: "content/blog/cars-and-coffee-march-2026.md"
      provides: "Sample published blog post with frontmatter and Cloudinary image URLs"
      contains: "status: published"
    - path: "content/blog/first-track-day.md"
      provides: "Sample draft blog post (should be skipped in build)"
      contains: "status: draft"
    - path: "scripts/build-blog.js"
      provides: "Prebuild script: markdown -> static HTML pages"
      min_lines: 100
      exports: ["buildBlog"]
    - path: "vite.config.js"
      provides: "Dynamic blog entry point discovery via glob"
      contains: "getBlogEntries"
  key_links:
    - from: "scripts/build-blog.js"
      to: "content/blog/*.md"
      via: "readdir + gray-matter + marked"
      pattern: "matter\\(|marked\\("
    - from: "vite.config.js"
      to: "blog/*/index.html"
      via: "readdirSync glob"
      pattern: "getBlogEntries"
    - from: "package.json"
      to: "scripts/build-blog.js"
      via: "prebuild script"
      pattern: "build-blog"
---

<objective>
Create the blog data pipeline: sample markdown posts with YAML frontmatter, a prebuild script that generates complete static HTML pages, and Vite config updates for dynamic blog page discovery.

Purpose: Establish the blog foundation so that markdown files in git become fully rendered static HTML pages at /blog/post-slug after build.
Output: Two sample posts, build-blog.js script, updated vite.config.js, working end-to-end build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-blog-data-pipeline-post-pages/08-CONTEXT.md
@.planning/phases/08-blog-data-pipeline-post-pages/08-RESEARCH.md

@scripts/build-gallery-data.js (existing prebuild pattern to follow)
@vite.config.js (current Vite config -- needs blog entry points added)
@package.json (needs gray-matter + marked deps, prebuild script update)
@index.html (nav and footer HTML to replicate in blog template)
@.gitignore (needs blog/ output directory added)

<interfaces>
<!-- Key code the executor needs from existing codebase -->

From index.html -- Nav HTML (lines 41-80, use absolute links for blog):
```html
<header class="nav" id="nav">
  <div class="nav__container">
    <a href="#hero" class="nav__logo">
      <span class="nav__monogram">DB</span>
      <span class="nav__wordmark">David Bradley</span>
    </a>
    <ul class="nav__links">
      <li><a href="#gallery" class="nav__link">Gallery</a></li>
      <li><a href="#video" class="nav__link">Video</a></li>
      <li><a href="#about" class="nav__link">About</a></li>
      <li><a href="#bts" class="nav__link">BTS</a></li>
      <li><a href="#contact" class="nav__link">Contact</a></li>
    </ul>
    <a href="#contact" class="nav__cta btn btn--primary">Book a Shoot</a>
    <button class="nav__hamburger" aria-label="Menu" aria-expanded="false">
      <span></span><span></span><span></span>
    </button>
  </div>
  <div class="nav__overlay">
    <ul>
      <li><a href="#gallery" class="nav__overlay-link">Gallery</a></li>
      <li><a href="#video" class="nav__overlay-link">Video</a></li>
      <li><a href="#about" class="nav__overlay-link">About</a></li>
      <li><a href="#bts" class="nav__overlay-link">BTS</a></li>
      <li><a href="#contact" class="nav__overlay-link">Contact</a></li>
    </ul>
    <a href="#contact" class="nav__overlay-cta btn btn--primary">Book a Shoot</a>
  </div>
</header>
```

From index.html -- Footer HTML (lines 331-353):
```html
<footer class="footer" id="footer">
  <div class="footer__container">
    <div class="footer__brand">
      <span class="footer__name">David Bradley</span>
      <span class="footer__tagline">Where Speed Meets Art</span>
      <span class="footer__copyright">&copy; 2026 David Bradley</span>
    </div>
    <nav class="footer__nav">
      <a href="#gallery" class="footer__link">Gallery</a>
      <a href="#video" class="footer__link">Video</a>
      <a href="#about" class="footer__link">About</a>
      <a href="#bts" class="footer__link">BTS</a>
      <a href="#contact" class="footer__link">Contact</a>
    </nav>
    <div class="footer__actions">
      <div class="footer__social">
        <a href="https://instagram.com/itz.dat.david" target="_blank" rel="noopener" class="footer__social-link">Instagram</a>
        <a href="https://tiktok.com/@itzdatdavid" target="_blank" rel="noopener" class="footer__social-link">TikTok</a>
      </div>
      <a href="#contact" class="btn btn--primary footer__cta">Book a Shoot</a>
    </div>
  </div>
</footer>
```

From vite.config.js -- Current config:
```javascript
import { defineConfig } from 'vite';
import { resolve } from 'path';

export default defineConfig({
  build: {
    rollupOptions: {
      input: {
        main: resolve(__dirname, 'index.html'),
        admin: resolve(__dirname, 'admin.html'),
      },
    },
  },
});
```

From package.json -- Current scripts:
```json
{
  "prebuild": "node scripts/build-gallery-data.js",
  "build": "vite build"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create sample posts, and build script</name>
  <read_first>
    - scripts/build-gallery-data.js (existing prebuild pattern -- follow env loading and file structure)
    - index.html (nav HTML lines 41-80, footer HTML lines 331-353 -- needed for blog page template)
    - package.json (current deps and scripts)
    - .planning/phases/08-blog-data-pipeline-post-pages/08-RESEARCH.md (patterns 1, 3, 4)
  </read_first>
  <files>
    package.json,
    content/blog/cars-and-coffee-march-2026.md,
    content/blog/first-track-day.md,
    scripts/build-blog.js
  </files>
  <action>
    1. Install gray-matter and marked as dev dependencies:
       `npm install --save-dev gray-matter marked`

    2. Create `content/blog/` directory and two sample posts:

       **content/blog/cars-and-coffee-march-2026.md** (status: published):
       ```yaml
       ---
       title: "Cars & Coffee March 2026"
       slug: cars-and-coffee-march-2026
       date: 2026-03-15
       cover: https://res.cloudinary.com/dl0atmtb7/image/upload/w_1600,f_auto,q_auto/v1/portfolio/jdm-supra-side
       excerpt: "What we captured at last weekend's Cars & Coffee meetup — JDM legends, early morning fog, and that one pristine R34 Skyline."
       tags:
         - Cars & Coffee
         - JDM
         - Events
       status: published
       ---
       ```
       Body: 3-4 paragraphs of automotive photography blog content with 2-3 inline Cloudinary image references using markdown syntax: `![Alt text](https://res.cloudinary.com/dl0atmtb7/image/upload/w_1200,f_auto,q_auto/v1/portfolio/IMAGE_ID)`. Use real Cloudinary public IDs from the portfolio folder (check build-gallery-data.js output pattern). Content should feel like a real shoot recap blog post.

       **content/blog/first-track-day.md** (status: draft):
       ```yaml
       ---
       title: "First Track Day of the Season"
       slug: first-track-day
       date: 2026-03-20
       cover: https://res.cloudinary.com/dl0atmtb7/image/upload/w_1600,f_auto,q_auto/v1/portfolio/track-drift-action
       excerpt: "Behind the lens at the season opener — 200mph straights, tire smoke, and the art of freezing speed."
       tags:
         - Track Day
         - Motorsport
       status: draft
       ---
       ```
       Body: 2 paragraphs placeholder content. This post tests that drafts are skipped.

    3. Create `scripts/build-blog.js`:
       - Use ESM imports (project is `"type": "module"`)
       - Read all `.md` files from `content/blog/`
       - Parse each with `gray-matter` to extract frontmatter + content
       - Skip files where `frontmatter.status !== 'published'` (drafts)
       - Convert markdown to HTML with `marked`
       - Use a custom `marked.Renderer()` for images: wrap each `<img>` in a PhotoSwipe-compatible `<a>` tag with `data-pswp-width="1600"`, `data-pswp-height="1067"`, class `blog-post__image-link`, and `data-caption` set to the alt text. The `<img>` gets class `blog-post__image` and `loading="lazy"`.
       - Generate complete HTML pages using a template function `buildPageHTML(frontmatter, bodyHTML)`:
         - Full `<!DOCTYPE html>` with charset, viewport meta
         - `<title>${title} | David Bradley Photography</title>`
         - OG/Twitter meta tags using title, excerpt, cover image
         - Google Fonts preconnect + stylesheet link (same as index.html)
         - Nav HTML from index.html BUT with absolute links: logo href="/", all section links prefixed with "/" (e.g., `/#gallery`, `/#video`, `/#about`, `/#bts`, `/#contact`), "Book a Shoot" CTA href="/#contact", overlay links same pattern
         - Add a "Blog" link in the nav: `<li><a href="/blog" class="nav__link">Blog</a></li>` (after BTS, before Contact in both desktop and overlay)
         - Blog post article with:
           - `.blog-post__hero` div with inline `style="background-image: url('${cover}')"` for full-bleed cover
           - `.blog-post__hero-overlay` containing `<h1 class="blog-post__title">`, `<time class="blog-post__date">` with formatted date (e.g., "March 15, 2026"), and `.blog-post__tags` with tag spans
           - `.blog-post__content` div containing the rendered markdown HTML
         - Footer HTML from index.html with same absolute link treatment (/#gallery, etc.), add Blog link to footer nav too
         - `<script type="module" src="/src/blog/blog-post.js"></script>` at end of body (absolute path so Vite resolves it)
       - Write each page to `blog/${slug}/index.html` (creating directories recursively)
       - Log output: "Built blog: {slug}" for each, and "Skipped draft: {slug}" for drafts
       - Also generate a `blog/posts.json` file listing all published posts metadata (title, slug, date, cover, excerpt, tags) in reverse-chronological order -- Phase 10 will use this for the listing page

    4. Update `package.json` prebuild script:
       `"prebuild": "node scripts/build-gallery-data.js && node scripts/build-blog.js"`
  </action>
  <verify>
    <automated>cd /Users/davidbradley/Projects/automotive-portfolio && node scripts/build-blog.js && test -f blog/cars-and-coffee-march-2026/index.html && ! test -f blog/first-track-day/index.html && grep -q "Cars &amp; Coffee March 2026" blog/cars-and-coffee-march-2026/index.html && grep -q "data-pswp-width" blog/cars-and-coffee-march-2026/index.html && grep -q "cloudinary" blog/cars-and-coffee-march-2026/index.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `content/blog/cars-and-coffee-march-2026.md` exists with `status: published` and Cloudinary image URLs
    - `content/blog/first-track-day.md` exists with `status: draft`
    - `node scripts/build-blog.js` produces `blog/cars-and-coffee-march-2026/index.html` but NOT `blog/first-track-day/index.html`
    - Generated HTML contains `<h1>` with post title, `<time>` with formatted date, cover image background, tag spans
    - Generated HTML images wrapped in `<a>` with `data-pswp-width="1600"` and `data-pswp-height="1067"` and class `blog-post__image-link`
    - Generated HTML has nav with absolute links (`/#gallery`, `/#video`, etc.) and a Blog link (`/blog`)
    - Generated HTML has footer with absolute links
    - Generated HTML has `<script type="module" src="/src/blog/blog-post.js">`
    - Generated HTML has OG meta tags with post title, excerpt, and cover image
    - `blog/posts.json` contains array with one published post's metadata
    - `package.json` has gray-matter and marked in devDependencies
    - `package.json` prebuild includes `&& node scripts/build-blog.js`
  </acceptance_criteria>
  <done>Build script generates complete HTML from sample markdown, skips drafts, images are PhotoSwipe-ready, nav/footer use absolute links with Blog link added</done>
</task>

<task type="auto">
  <name>Task 2: Update Vite config and gitignore for blog pages</name>
  <read_first>
    - vite.config.js (current config to extend)
    - .gitignore (add blog/ output directory)
    - .planning/phases/08-blog-data-pipeline-post-pages/08-RESEARCH.md (Pattern 2: dynamic Vite input discovery)
  </read_first>
  <files>
    vite.config.js,
    .gitignore
  </files>
  <action>
    1. Update `vite.config.js` to discover blog HTML files dynamically:
       - Import `readdirSync` and `existsSync` from `node:fs`
       - Add a `getBlogEntries()` function that:
         - Checks if `blog/` directory exists (returns empty object if not -- handles fresh clone before first prebuild)
         - Reads all subdirectories in `blog/`
         - For each subdirectory, checks if `index.html` exists
         - Returns an object mapping `blog/${slug}` to the absolute path of `blog/${slug}/index.html`
       - Spread `...getBlogEntries()` into `rollupOptions.input` alongside existing `main` and `admin` entries

       Final config shape:
       ```javascript
       import { defineConfig } from 'vite';
       import { resolve } from 'path';
       import { readdirSync, existsSync } from 'node:fs';

       function getBlogEntries() {
         const blogDir = resolve(__dirname, 'blog');
         if (!existsSync(blogDir)) return {};
         const entries = {};
         const slugs = readdirSync(blogDir, { withFileTypes: true })
           .filter(d => d.isDirectory())
           .map(d => d.name);
         for (const slug of slugs) {
           const htmlPath = resolve(blogDir, slug, 'index.html');
           if (existsSync(htmlPath)) {
             entries[`blog/${slug}`] = htmlPath;
           }
         }
         return entries;
       }

       export default defineConfig({
         build: {
           rollupOptions: {
             input: {
               main: resolve(__dirname, 'index.html'),
               admin: resolve(__dirname, 'admin.html'),
               ...getBlogEntries(),
             },
           },
         },
       });
       ```

    2. Add `blog/` to `.gitignore` (generated output, like dist/):
       Add a comment section after the existing entries:
       ```
       # Generated blog pages (built from content/blog/*.md)
       blog/
       ```

    3. Verify full build works:
       Run `npm run build` and confirm `dist/blog/cars-and-coffee-march-2026/index.html` exists in the output.
  </action>
  <verify>
    <automated>cd /Users/davidbradley/Projects/automotive-portfolio && node scripts/build-blog.js && npm run build 2>&1 | tail -5 && test -f dist/blog/cars-and-coffee-march-2026/index.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `vite.config.js` contains `getBlogEntries` function that globs `blog/*/index.html`
    - `vite.config.js` spreads blog entries into `rollupOptions.input`
    - `vite.config.js` handles missing `blog/` directory gracefully (returns empty object)
    - `.gitignore` includes `blog/` entry
    - `npm run build` succeeds without errors
    - `dist/blog/cars-and-coffee-march-2026/index.html` exists after build
    - No draft post pages in dist/ (first-track-day should not exist)
  </acceptance_criteria>
  <done>Vite discovers blog pages dynamically, full build produces blog HTML in dist/, blog/ output is gitignored</done>
</task>

</tasks>

<verification>
1. `node scripts/build-blog.js` generates blog HTML for published posts only
2. `npm run build` succeeds end-to-end with blog pages in dist/
3. Generated HTML has correct nav (absolute links + Blog link), footer, OG meta, PhotoSwipe image markup
4. `blog/posts.json` has published post metadata
5. Draft post (first-track-day) is not generated
</verification>

<success_criteria>
- Blog markdown files exist in content/blog/ with proper frontmatter schema
- Build script converts markdown to complete static HTML with nav, footer, OG tags
- Vite config dynamically discovers and processes blog pages
- `npm run build` produces dist/blog/cars-and-coffee-march-2026/index.html
- No draft posts in output
</success_criteria>

<output>
After completion, create `.planning/phases/08-blog-data-pipeline-post-pages/08-01-SUMMARY.md`
</output>
