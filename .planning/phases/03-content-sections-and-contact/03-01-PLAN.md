---
phase: 03-content-sections-and-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/main.js
  - src/components/video.css
  - src/components/video.js
  - src/components/about.css
  - src/components/bts.css
autonomous: true
requirements:
  - VID-01
  - VID-02
  - VID-03
  - VID-04
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - ABOUT-04
  - BTS-01
  - BTS-02
  - BTS-03

must_haves:
  truths:
    - "Video section shows a lite-youtube-embed facade with play button thumbnail at 16:9 aspect ratio"
    - "About section displays a silhouette avatar with purple border and compelling first-person bio text in two-column layout"
    - "BTS section shows a 2x2 grid of images with storytelling captions"
    - "About CTA button scrolls to contact section"
  artifacts:
    - path: "src/components/video.css"
      provides: "Video section responsive styles with 16:9 container"
      contains: "aspect-ratio"
    - path: "src/components/video.js"
      provides: "lite-youtube-embed import and initialization"
      contains: "lite-youtube-embed"
    - path: "src/components/about.css"
      provides: "About section two-column layout with avatar styling"
      contains: "about__photo"
    - path: "src/components/bts.css"
      provides: "BTS 2x2 grid layout with caption styling"
      contains: "bts__grid"
  key_links:
    - from: "src/components/video.js"
      to: "lite-youtube-embed"
      via: "npm import"
      pattern: "import.*lite-youtube-embed"
    - from: "src/main.js"
      to: "src/components/video.js"
      via: "import and initVideo()"
      pattern: "initVideo"
    - from: "index.html"
      to: "#contact"
      via: "about CTA href"
      pattern: "about__cta.*#contact"
---

<objective>
Build the three content-display sections: video reel with lite-youtube-embed facade, about section with avatar and bio, and behind-the-scenes image grid with captions.

Purpose: Complete the storytelling arc between gallery and contact -- visitors can watch video content, learn about David, and see behind-the-scenes work.
Output: Three fully styled, responsive sections populated with placeholder content.
</objective>

<execution_context>
@/Users/davidbradley/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidbradley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-sections-and-contact/03-CONTEXT.md
@.planning/phases/03-content-sections-and-contact/03-RESEARCH.md

@index.html
@src/main.js
@src/style.css
@src/tokens.css

<interfaces>
<!-- Existing patterns from the codebase that the executor must follow -->

From src/main.js (component registration pattern):
```javascript
import './components/hero.css';
import './components/nav.css';
import './components/gallery.css';
import { initHero } from './components/hero.js';
import { initNav } from './components/nav.js';
import { initGallery } from './components/gallery.js';

initHero();
initNav();
initGallery();
```

From src/style.css (section and button patterns):
```css
.section { /* padding, max-width container */ }
.section__heading { /* Orbitron, ALL CAPS, centered */ }
.btn--primary { /* purple accent button */ }
```

From index.html (existing section stubs):
```html
<section class="section" id="video">
  <h2 class="section__heading">Video</h2>
</section>
<section class="section" id="about">
  <h2 class="section__heading">About</h2>
</section>
<section class="section" id="bts">
  <h2 class="section__heading">Behind the Scenes</h2>
</section>
```

Available gallery images for BTS reuse (in public/images/gallery/):
- jdm-street-racer-01.webp
- euro-grand-tourer-02.webp
- track-rally-spec-03.webp
- supercar-exotic-red-01.webp
(and 25 more WebP files)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lite-youtube-embed and build video + about sections</name>
  <files>
    package.json
    src/components/video.css
    src/components/video.js
    src/components/about.css
    index.html
    src/main.js
  </files>
  <action>
1. Install lite-youtube-embed: `npm install lite-youtube-embed`

2. Create `src/components/video.js`:
   - Import `lite-youtube-embed/src/lite-yt-embed.css` and `lite-youtube-embed/src/lite-yt-embed.js`
   - Export `initVideo()` function (custom element self-registers on import, function can be a no-op or log readiness)

3. Create `src/components/video.css`:
   - `.video` section styling matching existing `.section` patterns
   - `.video__description` -- Space Grotesk body text, centered, max-width ~600px, color using `--color-text-secondary` or similar muted token
   - `.video__container` -- max-width 800px, centered, `aspect-ratio: 16/9`, border-radius 8px, overflow hidden
   - Responsive: container goes full-width on mobile with smaller side padding
   - Style the lite-youtube custom element to fill its container (width: 100%, display: block)

4. Create `src/components/about.css`:
   - `.about__content` -- CSS flexbox/grid, two-column on desktop (photo left ~300px, text right flex-1), gap using spacing tokens. Stacks on mobile at 768px breakpoint.
   - `.about__photo-wrap` -- centered on mobile
   - `.about__photo` -- 200px x 200px circle (border-radius: 50%), 3px solid purple accent border (`--color-accent`), background `--color-surface` or `#1e1e2e`, display flex align/justify center for the SVG icon
   - SVG silhouette: inline SVG of a camera-holding photographer silhouette, ~120px, fill with `--color-text-secondary` or muted gray
   - `.about__bio` -- paragraphs with comfortable line-height (1.7), Space Grotesk, `--color-text-primary`
   - `.about__cta` -- reuse `.btn.btn--primary`, margin-top spacing

5. Update `index.html` -- replace the `#video` stub with:
   ```html
   <section class="section video" id="video">
     <h2 class="section__heading">Video Reel</h2>
     <p class="video__description">
       See the cars come alive -- from golden hour shoots to track day action.
     </p>
     <div class="video__container">
       <lite-youtube
         videoid="dQw4w9WgXcQ"
         playlabel="Play: David Bradley automotive video reel"
       ></lite-youtube>
     </div>
   </section>
   ```
   (Use a real-looking placeholder videoid; user will swap later)

   Replace the `#about` stub with full about section:
   ```html
   <section class="section about" id="about">
     <h2 class="section__heading">About</h2>
     <div class="about__content">
       <div class="about__photo-wrap">
         <div class="about__photo">
           <!-- Inline SVG: photographer silhouette with camera -->
         </div>
       </div>
       <div class="about__bio">
         <p>I fell in love with cars before I could drive. Growing up, I spent weekends at local car meets with my dad, mesmerized by the curves of a 911, the rumble of a big-block V8, the way light played across freshly detailed paint. When I picked up a camera at sixteen, everything clicked -- I had found my way to freeze those moments forever.</p>
         <p>Today, I specialize in automotive photography that tells a story. Whether it is a concours-winning classic under studio lights or a track-prepped machine screaming through Turn 3, I chase the shot that makes you feel the speed, the craftsmanship, and the soul of the car. Every shoot is personal, every edit is intentional, and every image is built to make your car look the way it deserves.</p>
         <p>Based in the Midwest, available worldwide. Let's create something extraordinary together.</p>
         <a href="#contact" class="btn btn--primary about__cta">Let's Work Together</a>
       </div>
     </div>
   </section>
   ```
   (First-person passionate storyteller tone per user decision. User swaps real bio later.)

6. Update `src/main.js` -- add imports for video.css, about.css, video.js and call `initVideo()`. Do NOT add bts.css yet (Task 2).
  </action>
  <verify>
    <automated>cd /Users/davidbradley/Projects/automotive-portfolio && npm run build</automated>
  </verify>
  <done>Video section renders with lite-youtube-embed facade showing thumbnail and play button at 16:9. About section shows circular purple-bordered avatar with SVG silhouette, three paragraphs of first-person bio text in two-column layout on desktop, stacked on mobile. CTA button links to #contact. Build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build behind-the-scenes section with image grid and captions</name>
  <files>
    src/components/bts.css
    index.html
    src/main.js
  </files>
  <action>
1. Create `src/components/bts.css`:
   - `.bts__intro` -- introductory paragraph, centered, max-width ~600px, muted text color, margin-bottom spacing
   - `.bts__grid` -- CSS grid, `grid-template-columns: repeat(2, 1fr)` for 2x2 layout, gap using spacing tokens (e.g. `--space-md`). On mobile (<768px) stack to single column.
   - `.bts__item` -- position relative, overflow hidden, border-radius 8px
   - `.bts__img` -- width 100%, display block, aspect-ratio 4/3, object-fit cover
   - `.bts__caption` -- positioned as overlay at bottom of image (position absolute, bottom 0, left 0, right 0), semi-transparent dark background (rgba(0,0,0,0.7)), padding, Space Grotesk, small font size, white text
   - `prefers-reduced-motion` -- respect by skipping any hover transforms

2. Update `index.html` -- replace the `#bts` stub with:
   ```html
   <section class="section bts" id="bts">
     <h2 class="section__heading">Behind the Scenes</h2>
     <p class="bts__intro">
       Every great shot has a story behind it. Here is a glimpse into the process -- the early mornings, the patience, and the passion that goes into capturing automotive art.
     </p>
     <div class="bts__grid">
       <div class="bts__item">
         <img class="bts__img" src="/images/gallery/track-rally-spec-03.webp" alt="Setting up a low-angle shot of a rally car at dawn" loading="lazy" />
         <div class="bts__caption">Chasing golden hour at the track -- sometimes the best light means a 5 AM call time</div>
       </div>
       <div class="bts__item">
         <img class="bts__img" src="/images/gallery/jdm-drift-machine-03.webp" alt="Adjusting camera settings for a rolling shot" loading="lazy" />
         <div class="bts__caption">Dialing in the perfect shutter speed for a rolling shot -- 1/60s for just enough motion blur</div>
       </div>
       <div class="bts__item">
         <img class="bts__img" src="/images/gallery/supercar-carbon-fiber-05.webp" alt="Detail shot setup with portable lighting" loading="lazy" />
         <div class="bts__caption">Detail work demands patience -- sometimes one carbon fiber panel takes thirty minutes to light perfectly</div>
       </div>
       <div class="bts__item">
         <img class="bts__img" src="/images/gallery/euro-classic-roadster-05.webp" alt="Scouting locations with a classic roadster" loading="lazy" />
         <div class="bts__caption">Location scouting is half the art -- finding the backdrop that matches the car's personality</div>
       </div>
     </div>
   </section>
   ```
   (Narrative/emotional process-focused captions per user decision. Reuses existing gallery images as BTS stand-ins.)

3. Update `src/main.js` -- add `import './components/bts.css';` after the about.css import.
  </action>
  <verify>
    <automated>cd /Users/davidbradley/Projects/automotive-portfolio && npm run build</automated>
  </verify>
  <done>BTS section renders with heading, intro paragraph, and 2x2 image grid. Each image has a semi-transparent caption overlay with storytelling text. Grid stacks to single column on mobile. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `npm run dev` -- open browser and verify:
   - Video section: lite-youtube thumbnail visible with play button, 16:9 ratio, heading and description text
   - About section: circular avatar with purple border, SVG silhouette inside, bio text in two columns on desktop, stacked on mobile, CTA button present and links to #contact
   - BTS section: 2x2 image grid with caption overlays, stacks on mobile
3. Resize browser to mobile width -- all three sections stack properly
</verification>

<success_criteria>
- lite-youtube-embed installed and facade renders without loading YouTube iframe
- Video container maintains 16:9 ratio at all screen sizes
- About section has two-column desktop / stacked mobile layout with purple-bordered avatar
- Bio text uses first-person passionate storyteller tone
- BTS grid shows 4 images in 2x2 with storytelling captions
- All sections follow BEM naming convention
- Build produces zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections-and-contact/03-01-SUMMARY.md`
</output>
