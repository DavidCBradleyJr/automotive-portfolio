---
phase: 06-admin-authentication-upload
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - admin.html
  - src/admin/admin.js
  - src/admin/admin.css
  - src/admin/components/upload.js
  - src/admin/components/gallery-view.js
  - src/admin/components/settings.js
autonomous: false
requirements:
  - UPLOAD-01
  - UPLOAD-02
  - UPLOAD-03
  - UPLOAD-04
  - UPLOAD-05

must_haves:
  truths:
    - "David can drag-and-drop or file-pick JPEG/PNG/WebP images"
    - "David sees image previews before uploading"
    - "David sets a batch-level category and per-image caption and alt text"
    - "Image slug is auto-generated from category + caption, and editable"
    - "Upload shows per-image progress bars and success/error feedback"
    - "After batch upload, a toast shows and form clears for next batch"
    - "Gallery tab shows a read-only grid of existing Cloudinary images"
    - "Settings tab allows hero image selection from gallery"
  artifacts:
    - path: "src/admin/components/upload.js"
      provides: "Drag-and-drop upload zone, file preview, batch upload orchestration"
      exports: ["initUpload"]
    - path: "src/admin/components/gallery-view.js"
      provides: "Read-only gallery grid fetched from Cloudinary via function"
      exports: ["initGalleryView"]
    - path: "src/admin/components/settings.js"
      provides: "Hero image selection from gallery or custom upload"
      exports: ["initSettings"]
  key_links:
    - from: "src/admin/components/upload.js"
      to: "/.netlify/functions/upload-image"
      via: "fetch POST with Bearer token"
      pattern: "fetch.*upload-image"
    - from: "src/admin/components/upload.js"
      to: "/.netlify/functions/trigger-rebuild"
      via: "fetch POST after batch completes"
      pattern: "fetch.*trigger-rebuild"
    - from: "src/admin/components/upload.js"
      to: "src/admin/components/auth.js"
      via: "getToken() for JWT"
      pattern: "getToken"
---

<objective>
Build the upload UI with drag-and-drop, file previews, batch metadata, progress bars, and wiring to the Netlify Functions. Also create the read-only gallery view and hero image settings tab.

Purpose: This is the core user-facing functionality -- David's workflow for getting new photos onto the site without touching code.
Output: Fully functional upload experience, read-only gallery grid, and hero image selector.
</objective>

<execution_context>
@/Users/davidbradley/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidbradley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-authentication-upload/06-RESEARCH.md
@.planning/phases/06-admin-authentication-upload/06-01-SUMMARY.md

<interfaces>
<!-- From 06-01: auth.js exports -->
export function initAuth(onLogin, onLogout);
export async function getToken();  // Returns fresh JWT string
export function logout();
export function openLogin();

<!-- From 06-01: tabs.js exports -->
export function initTabs();

<!-- From 06-01: toast.js exports -->
export function showToast(type, message);  // type: 'success' | 'error' | 'info'

<!-- From 06-02: upload-image function endpoint -->
POST /.netlify/functions/upload-image
Authorization: Bearer <jwt>
Body: { image: "data:image/...", folder: "gallery/jdm", public_id: "slug", context: { caption, alt } }
Response: { public_id, url, width, height }

<!-- From 06-02: trigger-rebuild function endpoint -->
POST /.netlify/functions/trigger-rebuild
Authorization: Bearer <jwt>
Response: { message: "Rebuild triggered successfully" }

<!-- Cloudinary cloud name for URL construction -->
Cloud name: dl0atmtb7
Cloudinary URL pattern: https://res.cloudinary.com/dl0atmtb7/image/upload/{transforms}/{public_id}

<!-- Categories from build-gallery-data.js -->
const categoryLabels = {
  'jdm': 'JDM',
  'euro': 'Euro',
  'supercar': 'Supercar',
  'american-muscle': 'American Muscle',
  'track': 'Track/Motorsport',
};

<!-- Admin HTML panel sections from 06-01 -->
<section id="panel-upload">...</section>
<section id="panel-gallery">...</section>
<section id="panel-settings">...</section>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build upload component with drag-and-drop, previews, metadata, and batch upload</name>
  <files>src/admin/components/upload.js, admin.html, src/admin/admin.css</files>
  <read_first>
    - admin.html
    - src/admin/admin.js
    - src/admin/admin.css
    - src/admin/components/auth.js
    - src/admin/components/toast.js
    - scripts/build-gallery-data.js
  </read_first>
  <action>
1. Update `admin.html` -- replace the upload panel placeholder content with:
   ```html
   <section id="panel-upload" class="admin-panel-section admin-panel-section--active">
     <h2>Upload Photos</h2>

     <!-- Batch category selector -->
     <div class="upload-batch-settings">
       <label class="upload-label" for="batch-category">Category for this batch</label>
       <select id="batch-category" class="upload-select">
         <option value="">Select category...</option>
         <option value="jdm">JDM</option>
         <option value="euro">Euro</option>
         <option value="supercar">Supercar</option>
         <option value="american-muscle">American Muscle</option>
         <option value="track">Track/Motorsport</option>
       </select>
     </div>

     <!-- Drop zone -->
     <div class="upload-zone" id="upload-zone">
       <div class="upload-zone__icon">+</div>
       <p class="upload-zone__text">Drag photos here or</p>
       <label class="btn btn--primary upload-zone__btn">
         Browse Files
         <input type="file" id="file-input" multiple accept="image/jpeg,image/png,image/webp" hidden>
       </label>
       <p class="upload-zone__hint">JPEG, PNG, or WebP -- max 4 MB per file</p>
     </div>

     <!-- File queue (populated by JS) -->
     <div id="upload-queue" class="upload-queue"></div>

     <!-- Upload button -->
     <button id="upload-btn" class="btn btn--primary upload-action-btn" disabled>
       Upload All
     </button>
   </section>
   ```

2. Create `src/admin/components/upload.js`:

   Export `initUpload()` function that:

   a) **Drag-and-drop handling:**
      - Get #upload-zone, #file-input elements
      - dragover: preventDefault, add `.upload-zone--active`
      - dragleave: remove `.upload-zone--active`
      - drop: preventDefault, remove active, filter files to image/jpeg|png|webp, call addFiles()
      - file-input change: call addFiles(), reset input value
      - Reject files > 4 MB (4 * 1024 * 1024) -- show toast error with filename

   b) **File queue state:**
      - Maintain `fileQueue` array of objects: `{ file, preview, caption, alt, slug, status, progress }`
      - `addFiles(fileList)`: for each valid file, create entry with:
        - `preview`: URL.createObjectURL(file)
        - `slug`: auto-generated via `generateSlug(category, file.name)`
        - `caption`: empty string (user fills in)
        - `alt`: empty string (user fills in)
        - `status`: 'pending'
        - `progress`: 0
      - Re-render the queue after adding

   c) **Slug generation:**
      ```javascript
      function generateSlug(category, filename) {
        const base = filename
          .replace(/\.[^.]+$/, '')  // remove extension
          .toLowerCase()
          .replace(/[^a-z0-9]+/g, '-')
          .replace(/^-|-$/g, '')
          .slice(0, 40);
        return base || `${category}-${Date.now()}`;
      }
      ```

   d) **Queue rendering:**
      - Render #upload-queue with one card per file:
        ```html
        <div class="upload-item" data-index="N">
          <img class="upload-item__preview" src="{preview}" alt="Preview">
          <div class="upload-item__fields">
            <div class="upload-item__field">
              <label>Caption</label>
              <input type="text" class="upload-item__input" data-field="caption" value="" placeholder="e.g. Blue Supra at sunset">
            </div>
            <div class="upload-item__field">
              <label>Alt text</label>
              <input type="text" class="upload-item__input" data-field="alt" value="" placeholder="e.g. Blue Toyota Supra parked at sunset">
            </div>
            <div class="upload-item__field">
              <label>Image ID</label>
              <input type="text" class="upload-item__input" data-field="slug" value="{slug}">
            </div>
            <div class="upload-item__progress">
              <div class="upload-item__progress-bar" style="width: 0%"></div>
            </div>
            <div class="upload-item__status"></div>
          </div>
          <button class="upload-item__remove" data-index="N" title="Remove">&times;</button>
        </div>
        ```
      - Input change events update fileQueue entries (caption, alt, slug)
      - Remove button removes from fileQueue and re-renders
      - Enable #upload-btn when fileQueue.length > 0 and category is selected

   e) **Upload orchestration:**
      - #upload-btn click handler:
        1. Validate: category must be selected, at least one file in queue
        2. Disable upload button, show "Uploading..."
        3. Get fresh JWT via `getToken()`
        4. Upload files sequentially (one at a time to avoid rate limiting):
           - Convert file to base64 via FileReader.readAsDataURL()
           - POST to `/.netlify/functions/upload-image` with:
             - Authorization: `Bearer ${token}`
             - Body: `{ image: base64, folder: 'gallery/' + category, public_id: slug, context: { caption, alt } }`
           - Update progress bar to 100% on success, show checkmark status
           - On error: show X status, continue with next file
           - Refresh token via `getToken()` before each upload (prevents JWT expiry mid-batch)
        5. After all uploads complete:
           - If any succeeded, call `/.netlify/functions/trigger-rebuild` with Bearer token
           - Show toast: "{N} photos uploaded! Rebuilding site..." (success) or "Some uploads failed" (partial)
           - Clear fileQueue, re-render empty queue
           - Re-enable upload button

   f) **fileToBase64 helper:**
      ```javascript
      function fileToBase64(file) {
        return new Promise((resolve, reject) => {
          const reader = new FileReader();
          reader.onload = () => resolve(reader.result);
          reader.onerror = reject;
          reader.readAsDataURL(file);
        });
      }
      ```

3. Update `src/admin/admin.css` -- add upload-specific styles:
   - `.upload-batch-settings`: margin-bottom var(--space-lg)
   - `.upload-select`: dark background (#1a1a1a), color var(--color-text), border 1px solid var(--color-border), padding var(--space-sm), border-radius 4px, width 100%, max-width 300px
   - `.upload-label`: display block, margin-bottom var(--space-xs), color var(--color-text-muted), font-size var(--text-sm)
   - `.upload-zone`: border 2px dashed var(--color-border), border-radius 8px, padding var(--space-xl), text-align center, transition border-color 0.2s
   - `.upload-zone--active`: border-color var(--color-accent), background rgba(124, 58, 237, 0.05)
   - `.upload-zone__icon`: font-size 3rem, color var(--color-text-muted), margin-bottom var(--space-sm)
   - `.upload-zone__hint`: font-size var(--text-sm), color var(--color-text-muted), margin-top var(--space-sm)
   - `.upload-queue`: display flex, flex-direction column, gap var(--space-md), margin-top var(--space-lg)
   - `.upload-item`: display flex, gap var(--space-md), background #1a1a1a, border-radius 8px, padding var(--space-md), align-items flex-start
   - `.upload-item__preview`: width 120px, height 80px, object-fit cover, border-radius 4px, flex-shrink 0
   - `.upload-item__fields`: flex 1, display flex, flex-direction column, gap var(--space-xs)
   - `.upload-item__input`: background #0F0F0F, border 1px solid var(--color-border), color var(--color-text), padding var(--space-xs) var(--space-sm), border-radius 4px, width 100%
   - `.upload-item__progress`: height 4px, background #333, border-radius 2px, overflow hidden, margin-top var(--space-xs)
   - `.upload-item__progress-bar`: height 100%, background var(--color-accent), width 0%, transition width 0.3s
   - `.upload-item__remove`: background none, border none, color var(--color-text-muted), font-size 1.5rem, cursor pointer, padding var(--space-xs)
   - `.upload-item__status`: font-size var(--text-sm), color var(--color-text-muted)
   - `.upload-action-btn`: margin-top var(--space-lg), width 100%, max-width 300px
   - `.upload-action-btn:disabled`: opacity 0.5, cursor not-allowed
   - Mobile responsive: upload-item stacks vertically below 600px, preview goes full-width

4. Update `src/admin/admin.js` to import and call `initUpload()` inside the onLogin callback.
  </action>
  <verify>
    <automated>test -f src/admin/components/upload.js && grep -q "upload-zone" admin.html && grep -q "getToken" src/admin/components/upload.js && grep -q "upload-image" src/admin/components/upload.js && grep -q "trigger-rebuild" src/admin/components/upload.js && grep -q "fileToBase64" src/admin/components/upload.js && npx vite build 2>&1 | tail -3 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - admin.html has #upload-zone with file input accepting image/jpeg,image/png,image/webp
    - admin.html has #batch-category select with 5 category options
    - upload.js exports initUpload function
    - upload.js contains dragover/dragleave/drop event handlers on #upload-zone
    - upload.js creates previews via URL.createObjectURL
    - upload.js has generateSlug function
    - upload.js calls getToken() before each upload (JWT refresh)
    - upload.js POSTs to /.netlify/functions/upload-image with base64 + metadata
    - upload.js calls /.netlify/functions/trigger-rebuild after batch completes
    - upload.js validates file size < 4 MB
    - upload.js clears queue and shows toast after successful batch
    - admin.css has .upload-zone, .upload-item, .upload-item__progress-bar styles
    - Vite build succeeds
  </acceptance_criteria>
  <done>Upload tab has drag-and-drop zone, file previews, per-image metadata fields, editable slugs, progress bars, and batch upload wired to Netlify Functions with rebuild trigger</done>
</task>

<task type="auto">
  <name>Task 2: Build gallery view and settings tab components</name>
  <files>src/admin/components/gallery-view.js, src/admin/components/settings.js, admin.html, src/admin/admin.css, src/admin/admin.js</files>
  <read_first>
    - admin.html
    - src/admin/admin.js
    - src/admin/admin.css
    - src/admin/components/auth.js
    - scripts/build-gallery-data.js
  </read_first>
  <action>
1. Update `admin.html` -- replace gallery panel placeholder:
   ```html
   <section id="panel-gallery" class="admin-panel-section">
     <h2>Gallery</h2>
     <p class="admin-text-muted">View all images. Edit, delete, and reorder coming in Phase 7.</p>
     <div id="gallery-grid" class="admin-gallery-grid">
       <p class="admin-placeholder">Loading gallery...</p>
     </div>
   </section>
   ```

2. Update `admin.html` -- replace settings panel placeholder:
   ```html
   <section id="panel-settings" class="admin-panel-section">
     <h2>Settings</h2>
     <h3>Hero Image</h3>
     <p class="admin-text-muted">Select a gallery image to use as the hero background, or upload a custom hero image.</p>
     <div class="settings-hero">
       <div class="settings-hero__current">
         <p class="admin-label">Current hero:</p>
         <img id="current-hero" class="settings-hero__preview" src="" alt="Current hero image">
       </div>
       <div class="settings-hero__select">
         <p class="admin-label">Select from gallery:</p>
         <div id="hero-gallery-grid" class="admin-gallery-grid admin-gallery-grid--small"></div>
       </div>
       <div class="settings-hero__custom">
         <p class="admin-label">Or upload custom hero:</p>
         <label class="btn btn--secondary">
           Choose File
           <input type="file" id="hero-file-input" accept="image/jpeg,image/png,image/webp" hidden>
         </label>
       </div>
     </div>
   </section>
   ```

3. Create `src/admin/components/gallery-view.js`:
   Export `initGalleryView()`:
   - Fetch gallery images from Cloudinary using a lightweight approach:
     - Use the build-generated gallery data if available (fetch `/src/data/gallery-images.js` won't work at runtime)
     - Instead, create a simple Netlify Function or use the existing Cloudinary search
     - SIMPLEST APPROACH: Fetch the gallery data by fetching the public site's gallery HTML and extracting data, OR better yet, just render from Cloudinary search API via a small helper function
     - BEST APPROACH for Phase 6: Use the upload-image function pattern to add a `list-images` function... BUT that increases scope.
     - PRAGMATIC APPROACH: The gallery-view reads from a static JSON file generated during build. Since the admin loads after a build, the data is current. Add a fetch to `/gallery-data.json` (a build artifact).

   Actually, the simplest correct approach:
   - Modify `scripts/build-gallery-data.js` to ALSO output `dist/gallery-data.json` (or `public/gallery-data.json` so it's available in dev and prod)
   - gallery-view.js fetches `/gallery-data.json` and renders a grid

   HOWEVER -- this adds a file to Plan 02's territory. To keep it simple and self-contained:
   - gallery-view.js fetches the same Cloudinary search API that build-gallery-data.js uses, BUT via a new Netlify Function (to keep API secret server-side)

   MOST PRAGMATIC: Since this is a read-only preview (Phase 7 adds CRUD), keep it simple:
   - Add a `netlify/functions/list-images.mjs` function that calls Cloudinary search and returns JSON
   - gallery-view.js calls this function with Bearer token

   Create `netlify/functions/list-images.mjs`:
   ```javascript
   import { v2 as cloudinary } from 'cloudinary';

   cloudinary.config({
     cloud_name: process.env.CLOUDINARY_CLOUD_NAME,
     api_key: process.env.CLOUDINARY_API_KEY,
     api_secret: process.env.CLOUDINARY_API_SECRET,
   });

   export const handler = async (event, context) => {
     const headers = {
       'Access-Control-Allow-Origin': '*',
       'Access-Control-Allow-Headers': 'Content-Type, Authorization',
       'Content-Type': 'application/json',
     };

     if (event.httpMethod === 'OPTIONS') {
       return { statusCode: 204, headers, body: '' };
     }

     const { user } = context.clientContext || {};
     if (!user) {
       return { statusCode: 401, headers, body: JSON.stringify({ error: 'Unauthorized' }) };
     }

     try {
       const result = await cloudinary.search
         .expression('folder:gallery/*')
         .with_field('context')
         .sort_by('created_at', 'desc')
         .max_results(200)
         .execute();

       const images = result.resources.map(r => ({
         public_id: r.public_id,
         url: cloudinary.url(r.public_id, {
           fetch_format: 'auto', quality: 'auto', width: 400, crop: 'limit', secure: true,
         }),
         full_url: cloudinary.url(r.public_id, {
           fetch_format: 'auto', quality: 'auto', width: 2000, crop: 'limit', secure: true,
         }),
         width: r.width,
         height: r.height,
         category: extractCategory(r),
         caption: r.context?.custom?.caption || r.context?.caption || '',
         alt: r.context?.custom?.alt || r.context?.alt || '',
         created_at: r.created_at,
       }));

       return { statusCode: 200, headers, body: JSON.stringify({ images }) };
     } catch (err) {
       console.error('List images failed:', err);
       return { statusCode: 500, headers, body: JSON.stringify({ error: 'Failed to load gallery' }) };
     }
   };

   function extractCategory(resource) {
     if (resource.asset_folder) {
       const parts = resource.asset_folder.split('/');
       if (parts.length >= 2 && parts[0] === 'gallery') return parts[1];
     }
     const parts = resource.public_id.split('/');
     if (parts.length >= 3 && parts[0] === 'gallery') return parts[1];
     return 'uncategorized';
   }
   ```

   Then `gallery-view.js`:
   - Calls `/.netlify/functions/list-images` with Bearer token
   - Renders a grid of image cards showing thumbnail, category badge, caption, alt text
   - Each card: `<div class="admin-gallery-card"><img src="{url}"><div class="admin-gallery-card__meta"><span class="admin-gallery-card__category">{category}</span><p>{caption}</p></div></div>`
   - Shows "No images yet" if empty
   - Shows loading spinner while fetching

4. Create `src/admin/components/settings.js`:
   Export `initSettings()`:
   - Fetches gallery images from `/.netlify/functions/list-images` (same endpoint as gallery view)
   - Renders clickable thumbnails in #hero-gallery-grid
   - Clicking a thumbnail sets it as the hero:
     - Uploads/tags the image with a fixed public_id `site-config/hero` via the upload-image function
     - Actually, for hero selection from existing gallery images, we need to COPY or TAG, not re-upload
     - PRAGMATIC APPROACH: Use Cloudinary's `explicit` or `rename` API... but that needs another function
     - SIMPLEST: Create the hero as a Cloudinary resource with public_id `site-config/hero` by calling upload-image with the full Cloudinary URL of the selected image (Cloudinary accepts URLs as upload source)
     - Call upload-image with `{ image: selectedImage.full_url, folder: 'site-config', public_id: 'hero', context: {} }` with `overwrite: true`
     - Note: the upload-image function has `overwrite: false` -- update it to accept an `overwrite` parameter from the request body
   - Hero file input: same flow as upload but with fixed folder='site-config' and public_id='hero'
   - Shows current hero by fetching `site-config/hero` Cloudinary URL
   - Current hero preview: `https://res.cloudinary.com/dl0atmtb7/image/upload/w_600,c_limit,f_auto,q_auto/site-config/hero`
   - After hero change: show toast "Hero image updated! Will appear after next rebuild."
   - Trigger rebuild after hero change

5. Update `src/admin/admin.js`:
   - Import { initUpload } from './components/upload.js'
   - Import { initGalleryView } from './components/gallery-view.js'
   - Import { initSettings } from './components/settings.js'
   - In onLogin callback, after initTabs(), call initUpload(), initGalleryView(), initSettings()

6. Add gallery grid and settings styles to `src/admin/admin.css`:
   - `.admin-gallery-grid`: display grid, grid-template-columns repeat(auto-fill, minmax(200px, 1fr)), gap var(--space-md)
   - `.admin-gallery-grid--small`: grid-template-columns repeat(auto-fill, minmax(120px, 1fr))
   - `.admin-gallery-card`: background #1a1a1a, border-radius 8px, overflow hidden, cursor default
   - `.admin-gallery-card img`: width 100%, aspect-ratio 3/2, object-fit cover
   - `.admin-gallery-card__meta`: padding var(--space-sm)
   - `.admin-gallery-card__category`: display inline-block, background var(--color-accent), color white, font-size var(--text-sm), padding 2px 8px, border-radius 3px, margin-bottom var(--space-xs)
   - `.admin-text-muted`: color var(--color-text-muted), margin-bottom var(--space-md)
   - `.admin-label`: color var(--color-text-muted), font-size var(--text-sm), margin-bottom var(--space-xs)
   - `.settings-hero`: display flex, flex-direction column, gap var(--space-lg)
   - `.settings-hero__preview`: max-width 600px, border-radius 8px, border 1px solid var(--color-border)
   - `.settings-hero__select .admin-gallery-card`: cursor pointer, transition transform 0.2s, border 2px solid transparent
   - `.settings-hero__select .admin-gallery-card:hover`: transform scale(1.02), border-color var(--color-accent)
   - `.admin-placeholder`: color var(--color-text-muted), font-style italic
   - Loading spinner: simple CSS animation on `.admin-loading`

7. Important: Update `netlify/functions/upload-image.mjs` to accept an optional `overwrite` parameter:
   - Add `overwrite` to the destructured body: `const { image, folder, public_id, context: metadata, overwrite: shouldOverwrite } = JSON.parse(event.body);`
   - Pass to cloudinary: `overwrite: shouldOverwrite || false`
   - This allows hero image replacement without changing the upload function's default behavior

8. Also update `scripts/build-gallery-data.js` to fetch the hero image and write its URL to the gallery data or a separate config:
   - At the end of `main()`, after writing gallery-images.js, also try to fetch `site-config/hero` from Cloudinary
   - If found, write its optimized URL to `src/data/hero-config.js`:
     ```javascript
     export const heroConfig = {
       src: 'https://res.cloudinary.com/dl0atmtb7/image/upload/f_auto,q_auto/site-config/hero',
       cloudinary: true,
     };
     ```
   - If not found (no hero set via admin yet), skip -- the existing local hero image continues to work
   - This is a build-time operation so the public site stays static
  </action>
  <verify>
    <automated>test -f src/admin/components/upload.js && test -f src/admin/components/gallery-view.js && test -f src/admin/components/settings.js && test -f netlify/functions/list-images.mjs && grep -q "initUpload" src/admin/admin.js && grep -q "initGalleryView" src/admin/admin.js && grep -q "initSettings" src/admin/admin.js && grep -q "upload-zone" admin.html && grep -q "gallery-grid" admin.html && grep -q "settings-hero" admin.html && npx vite build 2>&1 | tail -3 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/admin/components/upload.js exports initUpload
    - upload.js handles dragover, dragleave, drop events on #upload-zone
    - upload.js calls URL.createObjectURL for file previews
    - upload.js has generateSlug function
    - upload.js POSTs to /.netlify/functions/upload-image with base64 data
    - upload.js calls /.netlify/functions/trigger-rebuild after batch
    - upload.js refreshes JWT via getToken() before each upload
    - upload.js validates files < 4 MB
    - gallery-view.js exports initGalleryView
    - gallery-view.js fetches /.netlify/functions/list-images
    - settings.js exports initSettings
    - settings.js renders clickable gallery grid for hero selection
    - netlify/functions/list-images.mjs exists with JWT validation
    - admin.html has #upload-zone, #gallery-grid, #hero-gallery-grid
    - admin.js imports and calls all three init functions
    - Vite build succeeds
  </acceptance_criteria>
  <done>Upload tab fully functional with drag-and-drop, previews, metadata, progress, and batch upload. Gallery tab shows read-only image grid. Settings tab allows hero image selection. All three tabs wired to Netlify Functions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <files>admin.html</files>
  <name>Task 3: Verify admin panel on deploy preview</name>
  <what-built>
    Complete admin panel with:
    - Netlify Identity login gate
    - Tabbed interface (Upload / Gallery / Settings)
    - Drag-and-drop photo upload with previews, metadata, and progress
    - Batch upload proxied through Netlify Function to Cloudinary
    - Read-only gallery view
    - Hero image selection from gallery
    - Automatic rebuild trigger after upload
  </what-built>
  <how-to-verify>
    1. Deploy to Netlify (commit and push, or `netlify deploy --prod`)
    2. PREREQUISITE: Ensure Netlify Identity is enabled and set to invite-only (Netlify Dashboard -> Site settings -> Identity)
    3. PREREQUISITE: Create a build hook named 'admin-upload' and set NETLIFY_BUILD_HOOK env var
    4. PREREQUISITE: Invite David's email via Netlify Identity dashboard
    5. Navigate to https://[site-url]/admin
    6. Verify: Login screen shows with "Log In" button -- no signup option visible
    7. Click "Log In" -- Netlify Identity widget appears
    8. Log in with invited email
    9. Verify: Tabbed interface shows Upload, Gallery, Settings tabs
    10. Upload tab: Drag a test JPEG into the drop zone -- verify preview appears
    11. Fill in caption and alt text, select a category
    12. Click "Upload All" -- verify progress bar fills and success toast appears
    13. Gallery tab: Verify the uploaded image appears in the grid
    14. Settings tab: Verify gallery images appear for hero selection
    15. Verify: Public site at / loads normally with no admin JS in the bundle
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
  <action>Deploy the site to Netlify and verify the complete admin panel works end-to-end: login, upload, gallery view, settings, and rebuild trigger.</action>
  <verify>Manual verification on deployed Netlify site</verify>
  <done>Admin panel fully functional on production Netlify deployment with auth, upload, gallery, and settings working end-to-end</done>
</task>

</tasks>

<verification>
- `npx vite build` succeeds with both entry points
- admin.html has upload zone with file input, gallery grid, settings panel
- upload.js calls /.netlify/functions/upload-image and /.netlify/functions/trigger-rebuild
- gallery-view.js calls /.netlify/functions/list-images
- All function endpoints validate JWT via context.clientContext.user
- No Cloudinary secrets exposed in client-side code
</verification>

<success_criteria>
- David can drag-and-drop photos, see previews, add metadata, and upload a batch
- Batch upload sends files to Cloudinary via authenticated Netlify Function
- After batch upload, site rebuild triggers automatically
- Gallery tab shows all uploaded images
- Settings tab allows hero image selection
- All admin functionality requires authentication
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-authentication-upload/06-03-SUMMARY.md`
</output>
