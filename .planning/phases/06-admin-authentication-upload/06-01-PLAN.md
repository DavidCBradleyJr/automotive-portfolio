---
phase: 06-admin-authentication-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - admin.html
  - src/admin/admin.js
  - src/admin/admin.css
  - src/admin/components/auth.js
  - src/admin/components/tabs.js
  - src/admin/components/toast.js
  - vite.config.js
  - netlify.toml
  - package.json
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "Navigating to /admin shows a Netlify Identity login gate"
    - "Only invited users can access the admin panel -- no signup option"
    - "After login, a tabbed interface shows Upload, Gallery, and Settings tabs"
    - "Admin JS is a separate bundle -- never loaded by public visitors"
    - "Admin panel uses the same dark theme as the public site"
  artifacts:
    - path: "admin.html"
      provides: "Admin HTML entry point with Netlify Identity widget and tab structure"
      contains: "netlify-identity-widget"
    - path: "src/admin/admin.js"
      provides: "Admin entry JS that initializes auth, tabs, and UI"
      exports: ["default entry point"]
    - path: "src/admin/admin.css"
      provides: "Admin-specific dark theme styles"
      contains: "admin"
    - path: "src/admin/components/auth.js"
      provides: "Netlify Identity init, login/logout, JWT retrieval"
      exports: ["initAuth", "getToken", "logout"]
    - path: "src/admin/components/tabs.js"
      provides: "Hash-based tab navigation"
      exports: ["initTabs"]
    - path: "vite.config.js"
      provides: "Multi-page build config with admin entry"
      contains: "admin"
  key_links:
    - from: "admin.html"
      to: "src/admin/admin.js"
      via: "script tag"
      pattern: "src/admin/admin.js"
    - from: "src/admin/admin.js"
      to: "src/admin/components/auth.js"
      via: "import"
      pattern: "import.*auth"
    - from: "vite.config.js"
      to: "admin.html"
      via: "rollupOptions.input"
      pattern: "admin.*admin.html"
---

<objective>
Create the admin panel shell with Netlify Identity authentication, tabbed layout, and Vite multi-page build configuration.

Purpose: Establishes the protected admin entry point that public visitors never see, with auth gating and the UI skeleton for upload/gallery/settings tabs.
Output: Working admin page at /admin with login gate, tab navigation, dark theme, and separate JS bundle.
</objective>

<execution_context>
@/Users/davidbradley/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidbradley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-authentication-upload/06-RESEARCH.md

<interfaces>
<!-- Design tokens from src/tokens.css (reuse in admin) -->
:root {
  --color-black-950: #0F0F0F;
  --color-violet-500: #7C3AED;
  --color-gray-200: #E0E0E0;
  --color-gray-400: #9CA3AF;
  --color-white: #FFFFFF;
  --color-bg: var(--color-black-950);
  --color-text: var(--color-gray-200);
  --color-text-muted: var(--color-gray-400);
  --color-accent: var(--color-violet-500);
  --font-display: 'Orbitron', sans-serif;
  --font-body: 'Space Grotesk', sans-serif;
}

<!-- Toast pattern from src/components/contact.js -->
function showToast(type, message) {
  // Creates div.toast.toast--{type} with role="status" aria-live="polite"
  // Slides in, auto-dismisses after 5s
}

<!-- Current vite.config.js -->
import { defineConfig } from 'vite';
export default defineConfig({});

<!-- Current netlify.toml -->
[build]
  command = "npm run build"
  publish = "dist"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install netlify-identity-widget and configure Vite multi-page build</name>
  <files>package.json, vite.config.js, netlify.toml</files>
  <read_first>
    - vite.config.js
    - netlify.toml
    - package.json
  </read_first>
  <action>
1. Install netlify-identity-widget:
   ```
   npm install netlify-identity-widget
   ```

2. Update vite.config.js to multi-page:
   ```javascript
   import { defineConfig } from 'vite';
   import { resolve } from 'path';

   export default defineConfig({
     build: {
       rollupOptions: {
         input: {
           main: resolve(__dirname, 'index.html'),
           admin: resolve(__dirname, 'admin.html'),
         },
       },
     },
   });
   ```

3. Update netlify.toml -- add Identity redirect for admin and ensure Functions directory is detected:
   ```toml
   [build]
     command = "npm run build"
     publish = "dist"
     functions = "netlify/functions"

   [[headers]]
     for = "/assets/*"
     [headers.values]
       Cache-Control = "public, max-age=31536000, immutable"

   [[headers]]
     for = "/*.html"
     [headers.values]
       X-Frame-Options = "DENY"
       X-Content-Type-Options = "nosniff"
       Referrer-Policy = "strict-origin-when-cross-origin"
   ```

   Note: Do NOT add a [functions] directory setting that conflicts -- use the standard `netlify/functions/` path.
  </action>
  <verify>
    <automated>grep -q "admin" vite.config.js && grep -q "netlify-identity-widget" package.json && grep -q "functions" netlify.toml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.js contains `admin: resolve(__dirname, 'admin.html')`
    - package.json contains "netlify-identity-widget" in dependencies
    - netlify.toml contains `functions = "netlify/functions"`
  </acceptance_criteria>
  <done>Vite builds both index.html and admin.html as separate entry points; netlify-identity-widget installed; netlify.toml configured for Functions</done>
</task>

<task type="auto">
  <name>Task 2: Create admin HTML, auth module, tabs, toast, styles, and entry JS</name>
  <files>admin.html, src/admin/admin.js, src/admin/admin.css, src/admin/components/auth.js, src/admin/components/tabs.js, src/admin/components/toast.js</files>
  <read_first>
    - src/tokens.css
    - src/components/contact.js
    - index.html
  </read_first>
  <action>
1. Create `admin.html` in project root (alongside index.html):
   - DOCTYPE html, lang="en", charset utf-8, viewport meta
   - Title: "Admin | David Bradley Photography"
   - Link Google Fonts (Orbitron + Space Grotesk) same as index.html
   - Link src/admin/admin.css
   - Script tag: `<script type="module" src="/src/admin/admin.js"></script>`
   - Body structure:
     ```html
     <div id="admin-app">
       <!-- Login screen (shown when not authenticated) -->
       <div id="login-screen" class="admin-login">
         <h1 class="admin-login__title">David Bradley</h1>
         <p class="admin-login__subtitle">Admin Panel</p>
         <button id="login-btn" class="btn btn--primary admin-login__btn">Log In</button>
       </div>

       <!-- Admin panel (hidden until authenticated) -->
       <div id="admin-panel" class="admin-panel" hidden>
         <header class="admin-header">
           <h1 class="admin-header__title">Admin Panel</h1>
           <button id="logout-btn" class="btn btn--secondary admin-header__logout">Log Out</button>
         </header>

         <nav class="admin-tabs">
           <button class="admin-tab admin-tab--active" data-tab="upload">Upload</button>
           <button class="admin-tab" data-tab="gallery">Gallery</button>
           <button class="admin-tab" data-tab="settings">Settings</button>
         </nav>

         <main class="admin-content">
           <section id="panel-upload" class="admin-panel-section admin-panel-section--active">
             <h2>Upload Photos</h2>
             <p class="admin-placeholder">Upload functionality loading...</p>
           </section>
           <section id="panel-gallery" class="admin-panel-section">
             <h2>Gallery</h2>
             <p class="admin-placeholder">Gallery view loading...</p>
           </section>
           <section id="panel-settings" class="admin-panel-section">
             <h2>Settings</h2>
             <p class="admin-placeholder">Settings loading...</p>
           </section>
         </main>
       </div>
     </div>
     ```

2. Create `src/admin/components/auth.js`:
   - Import netlifyIdentity from 'netlify-identity-widget'
   - Export `initAuth(onLogin, onLogout)`:
     - Call `netlifyIdentity.init()` with no signup option
     - On 'login' event: close widget, call `onLogin(user)`
     - On 'logout' event: call `onLogout()`
     - Check `netlifyIdentity.currentUser()` for existing session
   - Export `getToken()`:
     - Returns `await netlifyIdentity.refresh()` -- always fresh JWT
   - Export `logout()`:
     - Calls `netlifyIdentity.logout()`
   - Export `openLogin()`:
     - Calls `netlifyIdentity.open()` to show the Identity widget modal

3. Create `src/admin/components/tabs.js`:
   - Export `initTabs()`:
     - Hash-based tab navigation (#upload, #gallery, #settings)
     - `showTab(id)` toggles `.admin-tab--active` and `.admin-panel-section--active`
     - Click handlers on `.admin-tab` buttons set hash and show tab
     - On init, restore from URL hash or default to 'upload'
     - Listen to `hashchange` event for back/forward navigation

4. Create `src/admin/components/toast.js`:
   - Extract and adapt the toast pattern from `src/components/contact.js`
   - Export `showToast(type, message)` where type is 'success' | 'error' | 'info'
   - Same DOM structure: `div.toast.toast--{type}` with `role="status"` and `aria-live="polite"`
   - Slide-in via `.toast--visible`, auto-dismiss after 5 seconds
   - Add `.toast--info` variant with neutral color (same as text color)

5. Create `src/admin/admin.css`:
   - Import tokens: `@import '../tokens.css';`
   - Set `body` background to `var(--color-bg)`, color to `var(--color-text)`, font-family to `var(--font-body)`
   - Admin login screen: centered vertically and horizontally, Orbitron title
   - Admin header: flex row, space-between, padding var(--space-md) var(--space-lg)
   - Admin tabs: flex row, gap var(--space-sm), border-bottom 1px solid var(--color-border)
   - `.admin-tab`: background transparent, color var(--color-text-muted), padding var(--space-sm) var(--space-md), cursor pointer, border none, font-family var(--font-body)
   - `.admin-tab--active`: color var(--color-accent), border-bottom 2px solid var(--color-accent)
   - `.admin-panel-section`: display none
   - `.admin-panel-section--active`: display block, padding var(--space-lg)
   - `.btn--primary`: background var(--color-accent), color var(--color-white), padding var(--space-sm) var(--space-lg), border none, border-radius 4px, cursor pointer, font-family var(--font-body), font-weight 600
   - `.btn--secondary`: background transparent, color var(--color-text-muted), border 1px solid var(--color-border), padding var(--space-sm) var(--space-md), border-radius 4px, cursor pointer
   - Toast styles: same as public site (.toast, .toast--visible, .toast--success, .toast--error, .toast--info)
   - Responsive: max-width 1200px centered, padding adjustments for mobile

6. Create `src/admin/admin.js`:
   - Import { initAuth, openLogin, logout } from './components/auth.js'
   - Import { initTabs } from './components/tabs.js'
   - On DOMContentLoaded:
     - Get references: #login-screen, #admin-panel, #login-btn, #logout-btn
     - login-btn click -> openLogin()
     - logout-btn click -> logout()
     - initAuth(onLogin, onLogout):
       - onLogin: hide login-screen (hidden=true), show admin-panel (hidden=false), initTabs()
       - onLogout: show login-screen (hidden=false), hide admin-panel (hidden=true)
  </action>
  <verify>
    <automated>test -f admin.html && test -f src/admin/admin.js && test -f src/admin/admin.css && test -f src/admin/components/auth.js && test -f src/admin/components/tabs.js && test -f src/admin/components/toast.js && npx vite build --mode production 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - admin.html exists in project root with id="login-screen" and id="admin-panel"
    - admin.html contains script tag pointing to /src/admin/admin.js
    - src/admin/components/auth.js exports initAuth, getToken, logout, openLogin
    - src/admin/components/tabs.js exports initTabs with data-tab="upload", data-tab="gallery", data-tab="settings" handling
    - src/admin/components/toast.js exports showToast function
    - src/admin/admin.css imports tokens.css and sets --color-bg background
    - `npx vite build` succeeds producing both dist/index.html and dist/admin.html
    - Admin bundle is separate from public bundle (no admin code in main entry)
  </acceptance_criteria>
  <done>Admin panel has login gate via Netlify Identity, tabbed layout with Upload/Gallery/Settings, dark theme matching public site, toast system, and builds as a separate Vite entry point</done>
</task>

</tasks>

<verification>
- `npx vite build` succeeds and produces both `dist/index.html` and `dist/admin.html`
- `dist/admin.html` references admin-specific JS bundle (not main bundle)
- `grep -r "netlify-identity" dist/assets/` finds widget code only in admin bundle
- `admin.html` contains id="login-screen" and id="admin-panel" with hidden attribute
- Tab buttons have data-tab attributes for upload, gallery, settings
</verification>

<success_criteria>
- Vite multi-page build produces separate admin and public bundles
- Admin page shows login screen with Netlify Identity widget integration
- After login, tab navigation works between Upload, Gallery, and Settings
- Dark theme uses the same design tokens as the public site
- Public site bundle has zero admin code
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-authentication-upload/06-01-SUMMARY.md`
</output>
