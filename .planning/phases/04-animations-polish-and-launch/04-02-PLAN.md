---
phase: 04-animations-polish-and-launch
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - index.html
  - netlify.toml
  - src/data/gallery-images.js
autonomous: false
requirements: [PERF-01, PERF-02, PERF-03, PERF-04, PERF-05, PERF-06, PERF-07, PERF-08]
user_setup:
  - service: netlify
    why: "Site hosting and deployment"
    env_vars: []
    dashboard_config:
      - task: "Create Netlify account if none exists"
        location: "https://app.netlify.com/signup"
      - task: "Connect GitHub repo to Netlify for automatic deploys"
        location: "Netlify Dashboard -> Sites -> Import from Git"

must_haves:
  truths:
    - "All images have descriptive alt text (no empty alt on meaningful images)"
    - "Image filenames are descriptive (no IMG_1234 patterns)"
    - "Heading hierarchy is correct: one H1, logical H2/H3 structure"
    - "Open Graph and Twitter Card meta tags are present including og:image"
    - "Total JavaScript bundle is under 80KB gzip"
    - "The site is deployed and accessible on a Netlify URL"
  artifacts:
    - path: "netlify.toml"
      provides: "Netlify build configuration with caching headers"
      contains: "[build]"
    - path: "index.html"
      provides: "Complete meta tags for SEO and social sharing"
      contains: "twitter:card"
  key_links:
    - from: "netlify.toml"
      to: "npm run build"
      via: "build command configuration"
      pattern: 'command = "npm run build"'
    - from: "index.html"
      to: "/images/"
      via: "og:image meta tag pointing to a real image"
      pattern: "og:image"
---

<objective>
Audit and fix SEO/accessibility issues (meta tags, heading hierarchy, alt text), verify performance budget (JS < 80KB gzip, LCP < 2.5s), configure Netlify deployment, and deploy the site.

Purpose: This is the final step -- the site goes live on the public internet with proper SEO, performance, and social sharing metadata.
Output: Updated index.html with complete meta tags, netlify.toml config, deployed Netlify site.
</objective>

<execution_context>
@/Users/davidbradley/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidbradley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-animations-polish-and-launch/04-CONTEXT.md
@.planning/phases/04-animations-polish-and-launch/04-RESEARCH.md
@.planning/phases/04-animations-polish-and-launch/04-01-SUMMARY.md

@index.html
@src/main.js
@src/data/gallery-images.js
@package.json

<interfaces>
<!-- From 04-RESEARCH.md: netlify.toml configuration -->
```toml
[build]
  command = "npm run build"
  publish = "dist"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.html"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
```

<!-- From 04-RESEARCH.md: Twitter Card meta tags -->
```html
<meta property="og:image" content="/images/og-image.webp" />
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content="David Bradley | Automotive Photography" />
<meta name="twitter:description" content="Where Speed Meets Art -- premium automotive photography portfolio" />
<meta name="twitter:image" content="/images/og-image.webp" />
<meta name="description" content="David Bradley -- premium automotive photography portfolio. Where Speed Meets Art." />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEO audit, meta tags, performance verification, and Netlify config</name>
  <files>index.html, netlify.toml, src/data/gallery-images.js</files>
  <action>
**A. Add/complete meta tags in `index.html` `<head>`:**
- Add `<meta name="description" content="David Bradley — premium automotive photography portfolio. Where Speed Meets Art.">` if not present
- Add `<meta property="og:image" content="/images/og-image.webp" />` -- use the hero placeholder image as the OG image. Copy `/images/hero-placeholder.webp` to `/images/og-image.webp` (or just reference hero-placeholder.webp directly if it looks good at 1200x630 crop). If hero image is not ideal dimensions, pick the best landscape gallery image instead.
- Add Twitter Card tags:
  ```html
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="David Bradley | Automotive Photography" />
  <meta name="twitter:description" content="Where Speed Meets Art — premium automotive photography portfolio" />
  <meta name="twitter:image" content="/images/og-image.webp" />
  ```
- Add `<meta property="og:url" content="" />` (leave empty -- will be set after knowing Netlify URL, or omit)

**B. Heading hierarchy audit:**
- Verify index.html has exactly ONE `<h1>` (should be `.hero__name`)
- Verify all section headings are `<h2>` (Gallery, Video Reel, About, Behind the Scenes, Follow Along, Contact)
- If any h3 elements exist, verify they logically nest under h2
- Fix any violations found

**C. Image alt text audit:**
- Check all `<img>` tags in index.html have non-empty, descriptive `alt` attributes
- Check `src/data/gallery-images.js` -- verify all gallery image entries have descriptive `alt` values (not generic like "car photo")
- Fix any missing or generic alt text with descriptive alternatives

**D. Image filename audit:**
- Check `/images/` directory for any files matching `IMG_*.webp`, `DSC_*.webp`, or other camera default naming patterns
- All filenames should be descriptive (this was enforced by process-images.js, but verify)

**E. Create `netlify.toml` in project root:**
```toml
[build]
  command = "npm run build"
  publish = "dist"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.html"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
```

**F. Verify JS bundle size:**
- Run `npm run build`
- Check output for JS chunk sizes
- Verify total eagerly-loaded JS is under 80KB gzip (expected ~42KB per research)
- If over budget: consider dynamic-importing GSAP (lazy load after hero paint)

**G. Initialize git repo and push to GitHub:**
- `git init` (if not already a git repo)
- Create `.gitignore` with: `node_modules/`, `dist/`, `.DS_Store`
- `git add -A && git commit -m "Initial commit: David Bradley automotive photography portfolio"`
- Create GitHub repo: `gh repo create davidbradley-portfolio --public --source=. --push` (or similar -- use `gh` CLI)
- If `gh` is not available, provide instructions for manual GitHub repo creation
  </action>
  <verify>
    <automated>cd /Users/davidbradley/Projects/automotive-portfolio && npm run build 2>&1 | grep -E '(\.js|\.css)' | head -10 && grep -c 'twitter:card' index.html && grep -c 'og:image' index.html && test -f netlify.toml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All meta tags present (og:title, og:description, og:image, og:type, twitter:card, twitter:title, twitter:description, twitter:image, meta description). Heading hierarchy correct (one H1, H2s for sections). All images have descriptive alt text. No camera-default filenames. netlify.toml exists with build config and caching headers. JS bundle under 80KB gzip. Code pushed to GitHub.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to Netlify and verify live site</name>
  <files>netlify.toml</files>
  <action>
User connects the GitHub repository to Netlify for deployment. Claude has already automated everything possible: netlify.toml configuration, git repo initialization, GitHub push. The remaining step requires Netlify dashboard authentication which cannot be automated via CLI without prior auth.
  </action>
  <verify>
    <automated>cd /Users/davidbradley/Projects/automotive-portfolio && test -f netlify.toml && echo "Netlify config ready" || echo "MISSING netlify.toml"</automated>
  </verify>
  <done>Site is live on a Netlify subdomain URL, Lighthouse mobile performance >= 90, all sections render correctly with animations.</done>
  <what-built>
The complete David Bradley automotive photography portfolio is ready for deployment:
- All sections built (hero, gallery, video, about, BTS, social, contact, footer)
- Scroll animations (section reveals, hero parallax, gallery stagger)
- SEO meta tags and performance optimizations
- netlify.toml configured
- Code pushed to GitHub
  </what-built>
  <how-to-verify>
1. **Connect Netlify to GitHub:**
   - Go to https://app.netlify.com
   - Click "Add new site" -> "Import an existing project"
   - Select GitHub and authorize if needed
   - Find the repository that was just pushed
   - Build settings should auto-detect from netlify.toml (command: `npm run build`, publish: `dist`)
   - Click "Deploy"

2. **Verify deployed site:**
   - Visit the Netlify subdomain URL (e.g., https://xxx.netlify.app)
   - Scroll through all sections -- verify animations work smoothly
   - Test hero parallax effect
   - Test gallery filters and lightbox
   - Test contact form submission
   - Test mobile hamburger menu (use devtools responsive mode)
   - Verify footer layout and links

3. **Run Lighthouse audit on deployed URL:**
   - Open Chrome DevTools -> Lighthouse tab
   - Run audit on mobile preset
   - Verify Performance score >= 90
   - Verify LCP < 2.5s
   - Check for any critical warnings

4. **Test social sharing preview:**
   - Paste deployed URL into https://cards-dev.twitter.com/validator or Facebook sharing debugger
   - Verify image and title appear correctly (may need absolute URL update for og:image after knowing the Netlify subdomain)
  </how-to-verify>
  <resume-signal>Type "approved" if site looks good and Lighthouse >= 90, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds, JS bundle < 80KB gzip
2. `grep -c 'twitter:card' index.html` returns 1
3. `grep -c 'og:image' index.html` returns 1
4. `netlify.toml` exists with correct build config
5. Site deployed and accessible on Netlify URL
6. Lighthouse mobile performance >= 90
</verification>

<success_criteria>
- All PERF requirements met: Lighthouse >= 90, LCP < 2.5s, JS < 80KB, descriptive alt text, descriptive filenames, correct heading hierarchy, OG + Twitter meta tags
- Site deployed on Netlify with git-based CI/CD
- Social sharing previews show correct title, description, and image
</success_criteria>

<output>
After completion, create `.planning/phases/04-animations-polish-and-launch/04-02-SUMMARY.md`
</output>
